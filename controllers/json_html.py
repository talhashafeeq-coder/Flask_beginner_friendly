from flask import Flask , jsonify , Blueprint
from flask_cors import CORS


auth_bp = Blueprint('auth', __name__)


app = Flask(__name__)
CORS(app)


tutorials_html = [
    {
        "id": 1,
        "title": "Introduction to HTML",
        "content": "<h1>What is HTML?</h1><br><ul><li>HTML stands for Hyper Text Markup Language</li><li>HTML is the standard markup language for creating Web pages</li><li>HTML describes the structure of a Web page</li><li>HTML consists of a series of elements</li><li>HTML elements tell the browser how to display the content</li>HTML elements label pieces of content such as 'this is a heading', 'this is a paragraph', 'this is a link', etc.</ul><h4>A Simple HTML Document :</h4><code>&lt;!DOCTYPE html&gt;<br>&lt;html&gt;<br>&lt;body&gt;<br><br>&lt;h1&gt;My First Heading&lt;/h1&gt;<br><br>&lt;p&gt;My first paragraph.&lt;/p&gt;<br><br>&lt;/body&gt;<br>&lt;/html&gt;</code><br><h6>Example Explained</h6><ul><li>The <code>&lt;!DOCTYPE html&gt;</code> declaration helps with browser compatibility</li><li>The <code>&lt;html&gt;</code> element is the root of an HTML page</li><li>The <code>&lt;body&gt;</code> element contains the visible page content</li><li>The <code>&lt;h1&gt;</code> element defines a large heading</li><li>The <code>&lt;p&gt;</code> element defines a paragraph</li></ul><h4>Web Browser :</h4>The purpose of a web browser (Chrome, Edge, Firefox, Safari) is to read HTML documents and display them correctly.<br>A browser does not display the HTML tags, but uses them to determine how to display the document:<br> <img src=\'https://www.w3schools.com/html/img_chrome.png' alt=\"Forest\"> <br><h4>HTML Page Structure</h4><br>Below is a visualization of an HTML page structure:<br> <img src = \'https://i.pinimg.com/236x/ad/a4/e1/ada4e1d0f71aec60b675d2fdb15f0cf4.jpg\' ><h4>HTML History</h4>Since the early days of the World Wide Web, there have been many verison of HTML:<br><ul><li><b>1989</b> -Tim Berners-Lee invented www</li><li><b>1991</b> -Tim Berners-Lee invented HTML</li><li><b>1993</b> -Dave Raggett drafted HTML+<li><b>1995</b> -HMTL Working Group defined HTML 2.0</li><li><b>1997</b> -W3C Recommendation; HTML 3.2</li><li><b>1999</b>W3C Recommendation; HTML 4.01</li><li><b>2000</b>W3C Recommendation 1.0</li></ul>",
         "example": "<h3>Example Explained</h3><ul><li>The <code>&lt;!DOCTYPE html&gt;</code> declaration helps with browser compatibility</li><li>The <code>&lt;html&gt;</code> element is the root of an HTML page</li><li>The <code>&lt;body&gt;</code> element contains the visible page content</li><li>The <code>&lt;h1&gt;</code> element defines a large heading</li><li>The <code>&lt;p&gt;</code> element defines a paragraph</li></ul>",
         "example" : "<h1>Heading 1</h1><br><h2>Heading 2</h2><br><h3>Heading 3</h3><br><h4>Heading 4</h4><br><h4>Heading 5</h4><br><h6>Heading 6</h6><br><h3>Example Explained</h3><ul><li>HTML headings are defined with the <code>&lt;h1&gt;</code> to <code>&lt;h6&gt;</code> tags.</li><li>The <code>&lt;h1&gt;</code> tag defines the most important heading.</li><li>The <code>&lt;h6&gt;</code> tag defines the least important heading.</li><li>Browsers display HTML headings with different font sizes and default spacing.</li></ul>"
    },   
    {
        "id": 2,
        "title": "HTML Editors",
        "content": "A Simple text editor is all you need to learn HTML<br><h4>Learn HTML Using Notepad or TextEdit :</h4>Web pages can be created and modifed by using professional HTML Editors<br>However, For learing HTML we recommend a simple text editor like Notedpad (pc) ,or TextEdit (Mac)<br>We believe that using a simple text editor is a good way to learn HTML.Follow the steps below to create your first web page with Notepad or TextEdit<br><h4>Step 1: Open Notepad (PC)</h4>Windows 8 or later:<br>Open the Start Screen (the window symbol at the bottom left on your screen). Type Notepad.<br>Windows 7 or earlier:<br>Open Start > Programs > Accessories > Notepad<h4>Step 2: Open TextEdit (Mac)</h4>Open Finder > Applications > TextEdit<br>Also change some preferences to get the application to save files correctly. In Preferences > Format > choose 'Plain Text'<br>Then under 'Open and Save', check the box that says 'Display HTML files as HTML code instead of formatted tex'.<br>Then open a new document to place the code.<h4>Step 3 :Write Some HTML</h4>Write or copy the following HTML Code into Notepad<code>&lt;!DOCTYPE html&gt;<br>&lt;html&gt;<br>&lt;head&gt;<br>&lt;title&gt;HTML Tutorial&lt;/title&gt;<br>&lt;/head&gt;<br>&lt;body&gt;<br>&lt;h1&gt;My First Heading&lt;/h1&gt;<br>&lt;p&gt;My first paragraph.&lt;/p&gt;<br>&lt;/body&gt;<br>&lt;/html&gt;</code><br> <img src=\"https://www.w3schools.com/html/img_notepad.png\" alt=\"Save As Object\" width=\"500\" height=\"341\"> <br><h4>Step 4: Save the HTML Page</h4><br>Save the file on your computer. Select File > Save as in the Notepad menu. <br>Name the file \"myfirstpage.html\" and set the encoding to UTF-8 (which is the default format).<br> <img src=\"https://www.w3schools.com/html/img_saveas.png\" alt=\"Save As Object\" width=\"500\" height=\"341\"> <br><h4>Step 5: View the HTML Page in Your Browser</h4><br>Open the saved HTML file in your favorite browser (double click on the file, or right-click - and choose \"Open with\").<br>The result will look much like this:<br><img src=\"https://www.w3schools.com/html/img_chrome.png\" alt=\"Chrome Object\" width=\"500\" height=\"341\">",
        "example": ""
    },
    {
        "id": 3,
        "title": "HTML Basic",
        "content": "In this chapter we will show some basic HTML examples.Don't worry if we use tags you have not learned about yet.<h4>HTML Documents :</h4>All HTML documents must start with a document type declaration:<code><!DOCTYPE html></code><br>The HTML document itself begins with <code><html></code> and ends with <code></html></code><br>The visible part of the HTML document is between <code><body></code> and <code></body></code>.<br><br><h6>Example</h6><h1>Heading 1</h1><br><h2>Heading 2</h2><br><h3>Heading 3</h3><br><h4>Heading 4</h4><br><h5>Heading 5</h5><br><h6>Heading 6</h6><br><h3>Example Explained</h3><ul><li>HTML headings are defined with the <code>&lt;h1&gt;</code> to <code>&lt;h6&gt;</code></li><li><h6>The <code>&lt;!DOCTYPE&gt;</code> Declaration</h6></li><li>The <code>&lt;!DOCTYPE&gt;</code> declaration represents the document type, and helps browsers to display web pages correctly.</li><li>It must only appear once, at the top of the page (before any HTML tags).</li><li><br>The <code>&lt;!DOCTYPE&gt;</code> declaration is not case sensitive.</li><li>The <code>&lt;!DOCTYPE&gt;</code> declaration for HTML5 is:<code><!DOCTYPE html></code></li></ul>The <code>&lt;!DOCTYPE&gt;</code> declaration for HTML5 is:<b>example : <code>&lt;!DOCTYPE&gt;</code></b><br> <h4>HTML Heading :</h4>HTML heading are defined with the  <code>&lt;h1&gt;</code> to  <code>&lt;h6&gt;</code> tags.<br> <code>&lt;h1&gt;</code> defines the most important heading ,  <code>&lt;h6&gt;</code> defines the least important heading.<br>Example :<code>&lt;h1&gt;</code><code><h1>This is heading 1</h1></code>",
        "example": "<h6>HTML Paragraps</h6> HTML paragraphs ares defined with the<code>&lt;p&gt;</code> tag.<br>Example : <code><p>This is a paragraph.</p><p>This is another paragraph.</p></code>"
    },
    {
        "id": 4,
        "title": "HTML Elements",
        "content": "An HTML element is defined by a start tag, some content , and an end tag.<br><h4>HTML Elements :</h4>The HTML element is everything from the start tag to the end tag: <code>&lt;tagname&gt;</code> Content goes here...<code>&lt;/tagname&gt;</code><br>Examples of some HTML elements:<br><code>&lt;h1&gt;</code>My Fisrt Heading <code>&lt;/h1&gt;</code><br><code>&lt;p&gt;</code>My First paragraph<code>&lt;/p&gt;</code> <br><h4>Nested HTML Elements :</h4><br>HTML elements can be nested (this means that elements can contain other elements).<br>All HTML documents consist of nested HTML element.<br>The following example contains four HTML element (<code>&lt;HTML&gt;</code>),<code>&lt;body&gt;</code>,<code>&lt;h1&gt;</code>,<code>&lt;p&gt;</code>):<br><b>Example Explaned</b><br>The <code>&lt;html&gt;</code> element is the root element and it defines the whole HTML document.<br>It has a start tag <code>&lt;html&gt;</code> and an end tag <code>&lt;/html&gt;</code><br>Then, inside the <code>&lt;html&gt;</code> elements there is a <code>&lt;body&gt;</code> element: <br> <code>&lt;body&gt;</code><br><code>&lt;h1&gt;</code> My First Heading <code>&lt;/h1&gt;</code><br> <code>&lt;p&gt;</code> My first paragraph <code>&lt;/p&gt;</code><br> <code>&lt;/body&gt;</code><br> The <code>&lt;body&gt;</code> element defines the document's body<br> It has a start tag <code>&lt;body&gt;</code> and an end tag <code>&lt;/body&gt;</code><br><h4>Never Skip the End Tag :</h4><br><b>Some html element will display correctly , even if you forget the end tag:</b><br><h4>Empty HTML Elements :</h4> HTML element with no content are called empty elements<br> The <code>&lt;br&gt;</code> tag defines a line break, and is an empty element without a closing tag:<h6>Example</h6><br><code>&lt;br&gt;</code><br><h4>HTML is Not Case Sensitive :</h4><br>HTML tags are not case sensitive <code>&lt;P&gt;</code> means the same as <code>&lt;p&gt;</code> <br> The HTML standard does not require lowercase tags, but W3C recommends lowercase in HTML, and demands lowercase for stricter document types like XHTML.",
        "example": "<code>&lt;h1&gt;This is a heading element&lt;/h1&gt;</code>"
    },
    {
        "id": 5,
        "title": "HTML Attributes",
        "content": "HTML attributes provide additional information about HTML elements.<br><h4>HTML Attributes :</h4><ul><li>All HTML attributes can have attributes</li><li>Attributes provider additional information about elements</li><li>Attributes provider <b>additional information</b> about elements</b></li><li>Attributes are always specified in <b>the start tah</b></li><li>Attributes usally come in name/value pairs like: name='value<li</li></ul><h4>The href Attribute :</h4>The <code>&lt;a&gt;</code> tag defines a hperlink.The <code>&lt;href&gt;</code> attribute specifies the URL of the page the link goes to:<br><h6>Example</h6><br><code>&lt;a href='https://www.w3schools.com/'&gt;This is a link&lt;/a&gt;</code><h4>The src Attribute :</h4>The <code>&lt;img&gt;</code> tag is used to embed an images in an html page.The <code>&lt;src&gt;</code> attribute specifies the path to the image to be displayed:<br><h6>Example</h6> <code>&lt;img src='img_girl.png'&gt;</code> <br> There are two ways to specify the URL in the <code>&lt;src&gt;</code> attribute:<ol><li><b>Absolute URL</b> - Links to an external image that is hosted on another website.Example : src ='https://www.example.com'.<br><b>Notes: External images might be under copyright.If you do not get permission to use it , you may be in violation of copyright laws.In addition you cannot external images,it can suddenly be removed or changed.</b></li><li><b>Relative URL</b> -Links to an image that is hosted within the website.Here, the URL does not include the domain name.If the URL begins without a slash(/), it will be relative to the current page.Example: src ='/images/img_girl.png'.</li></ol><hr><br><h4>The width and height Attributes :</h4>The <code>&lt;img&gt;</code> tag should also contain the <code>width</code> and <code>height</code> attributes, which specify the width and height of the image(in pixels)<br><code>&lt;img src='img_bio.jpg' width='500' height='600'&gt;</code><br> <h4>The alt Attribute :</h4>The required <code>&lt;alt&gt;</code> attribute for the <<code>&lt;img&gt;</code> tag specifies an alternate text for an image, if the image for some reason cannot be displayed. This can be due to a slow connection, or an error in the <code>&lt;src&gt;</code> attribute, or if the user uses a screen reader.<code>&lt;img src='img_girl.png' alt='Girl in a jacket'&gt;</code><br><h4>The style Attribute :</h4> The <code>&lt;style&gt;</code> attribute is used to add styles to an element, such as color , font, size , and more.<br><h6>Example</h6> <code>&lt;style ='color: red'&gt;</code><br><h4>The lang Attribute :</h4><br> You should always include the <code>lang</code> attribute inside the <code>&lt;html&gt;</code> tag ,to declare the language of the web page. This is meant to asist search engines and browsers.<br> The following example specifies English as the language<br><h4>The title Attribute :</h4>The <code>tittle</code> attribute defines some extra information about an element.The value of the tittle attribute will be displayed as a tooltip when you mouse over the element<br><h4>Chapter Summary :</h4><ul><li>All HTML element can have attribute</li><li>The <code>href</code> attribute of <code>&lt;a&gt;</code> specifies the path to the image to be displayed</li><li>The <code>width</code> and <code>height</code> attributes of </code></li></ul>",
        "example": "<code>&lt;a href='https://example.com'&gt;Click here to visit example.com&lt;/a&gt;</code>"
    },
    {
        "id": 6,
        "title": "HTML Heading",
        "content": "HTML heading are title or subtitle that you want to display on a webpage. <br><h6>Example</h6><code>&lt;h1&gt;Main Heading&lt;/h1&gt;<br>&lt;h2&gt;Subheading&lt;/h2&gt;<br>&lt;h3&gt;Sub-subheading&lt;/h3&gt;</code><br><h4>HTML headings :</h4><br>HTML heading are defined with the <code>&lt;h1&gt;</code> to <code>&lt;h6&gt;</code> tags.<br> <code>&lt;h1&gt;</code> defines the most important heading.<code>&lt;h6&gt;</code> defines the least important heading.<br><h6>Example</h6><code>&lt;h1&gt;Main Heading&lt;/h1&gt;<br>&lt;h2&gt;Subheading&lt;/h2&gt;<br>&lt;h3&gt;Sub-subheading&lt;/h3&gt;</code><br><h4>Heading Are Important :</h4>Search engines use the headings to index the structure and content of your web pages.<br>Users often skim a page by its headings. It is important to use headings to show the document structure.<br> <code>&lt;h1&gt;</code> headings should be used for main headings, followed by <code>&lt;h2&gt;</code> headings, then the less important <code>&lt;h3&gt;</code>, and so on.",
        "example": "<code>&lt;h1&gt;</code> Headning 1 <code>&lt;/h1&gt;</code><br>code>&lt;h2&gt;</code> Headning 2 <code>&lt;/h2&gt;</code>"
    },
    {
        "id": 7,
        "title": "HTML Paragraphs",
        "content": "A paragraph always starts on a new line , and is usually a block of text<br><h4>HTML Paragraphs :</h4>The HTML <code>&lt;p&gt;</code> element defines a paragraph.<br>A paragraph always starts on a new line, and browsers automatically add some white space (a margin) before and after a paragraph.<br><h6>Example</h6><code>&lt;p&gt;This is a paragraph.&lt;/p&gt;</code><br><code>&lt;p&gt;This is another paragraph.&lt;/p&gt;</code><br><h4>Paragraphs are Important :</h4>Search engines use the paragraphs to index the structure and content of your web pages.<br>Users often skim a page by its paragraphs. It is important to use paragraphs to show the document structure.<br> <code>&lt;p&gt;</code> paragraphs should be used for main paragraphs, followed by <code>&lt;p&gt;</code> paragraphs, then the less important <code>&lt;p&gt;</code>, and so on.",
        "example": "<code>&lt;p&gt;</code> This is a paragraph. <code>&lt;/p&gt;</code><br><code>&lt;p&gt;</code> This is another paragraph. <code>&lt;/p&gt;</code><br><h4>HTML Horizaontal Rules :</h4> <br> The <code>&lt;hr&gt;</code> tag defines a thematic break in an HTML page, and is most often displayed as a horizontal rule.<br> The <code>&lt;hr&gt;</code> element is used to separate content(or define a change) in an HTML page: <h6>Example</h6> <code>&lt;h1&gt;</code> This is Heading 1 <code>&lt;/h1&gt;</code><br><code>&lt;p&gt;</code> This is a paragraph. <code>&lt;/p&gt;</code><br><code>&lt;hr&gt;</code><br><code>&lt;h2&gt;</code> This is Heading 2 <code>&lt;/h2&gt;</code><br><code>&lt;p&gt;</code> This is another paragraph. <code>&lt;/p&gt;</code><br><code>&lt;hr&gt;</code><br><h4>HTML Line Breaks :</h4> The HTML <code>&lt;br&gt;</code> element defines a line break. <br> Use <code>&lt;br&gt;</code> if you want a line break (a new line) without starting a new paragraph:<br><h6>Example</h6><code>&lt;p&gt;</code> This is <code>&lt;br&gt;</code> a paragraph with line breaks.<code>&lt;/p&gt;</code> <br><h4>Solution - The HTML <pre> Element :</h4> The HTML <code>&lt;pre&gt;</code> element defines preformatted text.<br> The text inside a code>&lt;pre&gt;</code> element is displayed in a fixed-width font (usually Courier) , and it preserves both spaces and line breaks:<h6>Example</h6> <code>&lt;pre&gt;</code><br>My Bonnie lies over the ocean.<br>My Bonnie lies over the sea.<br>My Bonnie lies over the ocean.<br><code>&lt;/pre&gt;</code>  ",
        "example": "<code>&lt;p&gt;This is a paragraph that contains some text.&lt;/p&gt;</code>"
    },
    {
        "id": 8,
        "title": "HTML Style",
        "content": "The HTML <code>style</code> attribute is used to add style to an element, such as color, font , size and more.<h6>Example</h6><code>&lt;p style='color:red;'&gt;This paragraph is red.&lt;/p&gt;</code><br><h4>The HTML Style Attribute :</h4>Setting the style of an HTML element, can be done with the <code>style</code> attribute<br>The HTML <code>style</code> attribute has the following syntax:<h6>Example</h6><code>&lt;tagname style='property:value;'&gt;</code><h4>Background Color :</h4>The CSS <code>background-color</code> property defines the backgorund color for an HTML element.<h6>Example</h6>Set the background color for a page to powderblue:<br><code>&lt;body style='background-color:blue;'&gt; >&gt;</code><h4>Text Color :</h4>The CSS <code>color</code> property defines the text color for an HTML element:<h6>Example</h6> <code>&lt;p style='color:blue;'&gt;This paragraph is blue.&lt;/p&gt;</code><h4>Fonts :</h4>The CSS <code>font-family</code> property defines the font to be used for an HTML element:<h6>Example</h6><code>&lt;p style='font-family:courier;'&gt;This is a paragraph.&lt;/p&gt;</code><br><h4>Text Size :</h4>This CSS <code>font-size</code> property defines the text size for an HTML element:<h6>Example</h6><code>&lt;p style='font-size:300%'&gt;This is a paragraph.&lt;/p&gt;</code><h4>Text Alignment :</h4> The CSS <code>text-align</code> property defines the horizonal text alignment for an HTML element:<h6>Example</h6><code>&lt;p style='text-align:center;'&gt;Centered Heading&lt;/p&gt;</code><h4>Chapter Summary :</h4><ul><li>Use the <code>style</code> attribute for styling HTML elements</li><li>Use <code>background-color</code> for background color</li><li>Use <code>color</code> for text colors</li><li>Use <code>font-family</code> for text fonts</li><li>Use <code>font-size</code> for text sizes</li><li>Use <code>text-align</code> for text alignment</li></ul>",
        "example": "<code>&lt;p style='color:blue;'&gt;This paragraph is blue.&lt;/p&gt;</code>"
    },
    {
        "id": 9,
        "title": "HTML Formatting",
        "content": "HTML contain several elements for defining text with a special meaning<h4>HTML Formatting Elements :</h4>Formatting element were designed to display special types of text:<ul><li><code>&lt;b &gt;</code> Bold text</li><li><code>&lt;strong &gt;</code> Important text</li><li><code>&lt;i &gt;</code> Italic text</li> <li><code>&lt;em &gt;</code> Emphasized text</li><li><code>&lt;mark &gt;</code> Marked text</li><li><code>&lt;small &gt;</code> Smaller text</li><li><code>&lt;del &gt;</code> Deleted text</li><li><code>&lt;ins &gt;</code> Inserted text</li><li><code>&lt;sub &gt;</code> Subscript text</li><li><code>&lt;sup &gt;</code> Superscript text</li></ul><h4>HTML <code>&lt;b&gt;</code> and <code>&lt;strong&gt;</code> Elements :</h4>The HTML <code>&lt;b &gt;</code> element defines blod text, without any extra importance<h6>Example:</h6><code>&lt;b &gt;</code>This text is bold <code>&lt;/b &gt;</code><br><b>This text is bold</b><br>The HTML <code>&lt;strong &gt;</code> element defines important text, without any extra importance<h6>Example</h6><code>&lt;strong &gt;</code>This text is important <code>&lt;/strong &gt;</code> <br><strong>This text is important</strong> <h4>HTML <code>&lt;i &gt;</code> and <code>&lt;em &gt;</code> Elements :</h4>The HTML <code>&lt;i &gt;</code> element defines a part of text in an alternate voice or mood.<br>The content inside is typically displayed in italic<br><b>Tip:</b> The <code>&lt;i &gt;</code> tag is often used to indicate a technical term, a phrase from another language, a thought, a ship name, etc.<h6>Example</h6><code>&lt;i &gt;</code>This text is italic.<code>&lt;/i &gt;</code><br><i>This text is italic </i><br>The HTML <code>&lt;em &gt;</code> element defines emphasized text. The content inside is typically displayed in italic.<br><b>Tip:</b> A screen reader will pronounce the words in <code>&lt;em &gt;</code> with an example, using verbal stess.<h6>Example</h6><code>&lt;em &gt;</code> This text is emphasized<code>&lt;/em &gt;</code><br><em>This text is emphasized</em><h4>HTML <code>&lt;small &gt;</code> Element :</h4>The HTML <code>&lt;small &gt;</code> element defines smaller text:<h6>Example :</h4><code>&lt;small &gt;</code> This is some smaller text.<code>&lt;/small &gt;</code><br><small>This is some smaller text.</small><h4>HTML <code>&lt;mark &gt;</code> Element :</h4>The HTML <code>&lt;mark &gt;</code> element defines text that should be marked or highilighted:<h6>Example</h6><code>&lt;p &gt;</code> Do not forget to buy <code>&lt;mark&gt;</code> milk <code>&lt;/mark &gt;</code> today.<code>&lt;/p&gt;</code><p>Do not forget to buy <mark>milk</mark> today.</p><h4>HTML <code>&lt;del &gt;</code> Element :</h4> The HTML <code>&lt;del &gt;</code> element defines text that has been deleted from a document.Browsers will usually strike a line through deleted text:<h6>Example</h6><code>&lt;p &gt;</code>My Favorite color is <code>&lt;del &gt;</code> blue <code>&lt;/del&gt;</code> red.<code>&lt;/p&gt;</code><p> My Favorite color is <del>blue</del> red</p><h4>HTML <code>&lt;ins&gt;</code> Element :</h4>The HTML <code>&lt;ins &gt;</code> element defines a text that has been inserted into a document.Browsers will usually underline inserted text:<h6>Example</h6><code>&lt;p &gt;</code>My favourite color is <code>&lt;del &gt;</code> red <code>&lt;/del &gt;</code><code>&lt;ins &gt;</code> yellow <code>&lt;/ins&gt;</code>.<code>&lt;/p &gt;</code><p>My favourite color is <del>red</del><ins>yellow</ins></p><h4>HTML <code>&lt;sub&gt;</code> Element :</h4>The HTML <code>&lt;sub &gt;</code> element defines subscript text.Subscript appears half a character below the normal line, and is sometimes renders in a smaller font.Subscript text can be used for chemical formulas<h6>Example</h6><code>&lt;p &gt;</code> This is <code>&lt;sub &gt;</code> subscripted<code>&lt;/sub &gt;</code> text.<code>&lt;/p &gt;</code><br><p>This is <sub>subscripted</sub> text<h4>HTML <code>&lt;sup&gt;</code> Element :</h4>The HTML <code>&lt;sup&gt;</code> element defines superscript text.Superscript text appears half a character above the normal line, and is sometimes rendered in a smaller font.Superscript text can be used for footnotes.<h6>Example</h6><code>&lt;p &gt;</code>This is <code>&lt;sup &gt;</code> superscripted<code>&lt;/sup &gt;</code> text.<code>&lt;/p &gt;</code><br><p>This is <sup>superscripted</sup> text.",
        "example": "<code>&lt;b&gt;This is bold text&lt;/b&gt;</code>"
    },
    {
        "id": 10,
        "title": "HTML Quotations and Citation Elements",
        "content": "In this chapter we will go throuh the <code>&lt;blockquote&gt;</code>,<code>&lt;q&gt;</code>,<code>&lt;abbr&gt;</code>,<code>&lt;address&gt;</code>,<code>&lt;city&gt;</code>,<code>&lt;bdo&gt;</code> HTML elements<br><h4>HTML <code>&lt;blockquote&gt;</code> for Quotations :</h4> The HTML <code>&lt;blockquote&gt;</code> element defines a section that is quoted from another source<br> Browsers usually indend <code>&lt;blockquote&gt;</code> elements<h6>Example</h6><code>&lt;p&gt;</code>Here is q qoute from WWF's website:<code>&lt;/p&gt;</code><br><code>&lt;blockquote city='http://www.example.org' >&gt;</code>For 60 years, WWF has worked to help people and nature thrive. As the world's leading conservation organization, WWF works in nearly 100 countries. At every level, we collaborate with people around the world to develop and deliver innovative solutions that protect communities, wildlife, and the places in which they live.<code>&lt;/blockquote&gt;</code><br><h4>HTML <code>&lt;q&gt;</code> for short Quotations :</h5>The HTML <code>&lt;q&gt;</code> tag defines a short quataion.Browsers normally insert quotation marks around the quotation.<h6>Example</h6><code>&lt;p&gt;</code>WWF's goal is to: <code>&lt;q&gt;</code>Bulid a future where people live in harmony with nature.<code>&lt;/q&gt;</code>.<code>&lt;/p&gt;</code><br><h4>HTML <code>&lt;abbr&gt;</code> for abbreviations :</h4> The HTML <code>&lt;abbr&gt;</code> tag defines an abbreviation or an acronym, like 'HTML','CSS','Mr','ASAP','ATM'<br>Marking abbreviations can give useful information to browsers, translation systems and search-engines.<br><b>Tip:</b> Use the global title attribute to show the description for the abbreivation/acronym when you mouse over the element.<h6>Example</h6> <code>&lt;p&gt;</code>The <code>&lt;abbr title='World Health Organization' &gt;</code>WWF<code>&lt;/abbr&gt;</code> was founded in 1942.<code>&lt;/p&gt;</code><br><h4>HTML <code>&lt;address&gt;</code> for Contact information :</h4>The HTML <code>&lt;address&gt;</code> tag defines the contact information for the author/owner of a document or an article.<br>The contact information can be an email address,URL,pyhsical address,phone number,social media handle,etc<br>The text in the <code>&lt;address&gt;</code> add a line break before and after the <code>&lt;address&gt;</code> element.<br><h6>Example</h6><code>&lt;address&gt;</code><br>Written by John Doe.<br>Visit us at:<br>Example.com<br>Box 434 ,Iceland<br><code>&lt;/address&gt;</code><br><h4>HTML <code>&lt;city&gt;</code> for Work Title :</h4>The HTML <code>&lt;city&gt;</code> tag defines the title of a  work(e.g a book,a peom , a song , a movie)<br><b>Note:</b> A person's name is not the title of a work.<br>The text in the the <code>&lt;city&gt;</code> element usually render in italic<h6>Example</h6><code>&lt;city&gt;The Road Not Taken&lt;/city&gt;</code>",
        "example": "<code>&lt;q&gt;This is an inline quote&lt;/q&gt;</code>"
    },
    {
        "id": 11,
        "title": "HTML Comments",
        "content": "HTML comments are not displayed in the browser, but they can help document your HTML source code.<h4>HTML Comment Tag :</h4>You can add comments to your HTML source by using the following syntax:<h6>Example</h6><code>&lt;!-- write your comment here -- &gt;</code><br><b>Notic</b> that there is an exclamation point(!) in the start tag, but not in the end tag.<h4>Hide Content :</h4>Comments can be used to hide content.<br>This can be helpful if you hide content temporarily<h6>Example</h6><code>&lt;p &gt;</code>This is a paragraph.<code>&lt;/p &gt;</code><br><code>&lt;!-- &gt;</code> <code>&lt;p &gt;</code> this is another paragraph <code>&lt;/p &gt;</code> <code>&lt;-- &gt;</code>",
        "example": "<code>&lt;!-- This is an HTML comment --&gt;</code>"
    },
    {
        "id": 12,
        "title": "HTML Links",
        "content": "Links are found in nearly all web pages . Links allow users to click thier way from page to page.<h4>HTML Links - Hyperlinks :</h4>HTML links are hyperlinks.<br>You can click on a link and jump to another document.<br>When you move the mouse over a link, the mouse arrow will turn into a little hand.<br><h4>HTML Links - Syntax :</h4> The HTML <code>&lt;a &gt;</code> tag defines a hyperlink.It has the following syntax.<br><code>&lt;a href='url' &gt;</code> Link text <code>&lt;/a &gt;</code><br>The most important attribute of the <code>&lt;a &gt;</code> element is the <code>href</code> attributes, which indicates the link's destination.<br> The link text is the part that will be visible to the reader.<br>Clicking on the link text, will send the reader to the specified URL address<br><h6>Example</h6> This example shows how to create a link...<br><code>&lt;a href='https://www.example.com' &gt;</code> Visit Links!<code>&lt;/a &gt;</code><h4>HTML Links - The target Attribute :</h4>By y default, the linked page will be displayed in the current browser window. To change this, you must specify another target for the link. <br> The <code>target</code> attribute specifies where to opem the linked document.<br> The <code>target</code> attribute can have one of the following values:<ul><li><code>_self</code> - Default, Opens the document in the same window/tab as it was clicked</li><li><code>_blank</code> -Opens the document in a new window or tab</li><li><code>_parent</code> -Open the document in the parent frame </li><li><code>_top</code> -Opens the document in the full body of the window</li></ul>",
        "example": "<code>&lt;a href='https://example.com'&gt;Visit Example&lt;/a&gt;</code>"
    },
    {
        "id": 13,
        "title": "HTML Images",
        "content": "Images can improve the design and the appearance of a web page.<h4>HTML Images Syntax :</h4>The HTML <code>&lt;img&gt;</code> tag is used to embed an image in a web page.<br>Images are not technically inserted into a web page; images are linked to web pages. The <code>&lt;img&gt;</code> tag creates a holding space for the referenced image.<br>The <code>&lt;img&gt;</code> tag is empty, it contains attributes only, and does not have a closing tag.<br>The <code>src</code> attribute specifies the path to the image to be displayed.<h6>Example</h6><code>&lt;img src='image.jpg' alt='Sample image'&gt;</code><h4>HTML Images - The alt Attribute :</h4>The <code>alt</code> attribute provides an alternate text for an image, if the user for some reason cannot view it (because of slow connection, an error in the src attribute, or if the user uses a screen reader).<br>The value of the <code>alt</code> attribute should describe the image as precisely as possible.<h6>Example</h6><code>&lt;img src='image.jpg' alt='A beautiful sunset over the mountains'&gt;</code><br><h4>HTML Images - The width and height Attributes :</h4>The <code>width</code> and <code>height</code> attributes specify the width and height of the image (in pixels).<h6>Example</h6><code>&lt;img src='image.jpg' alt='Sample image' width='300' height='200'&gt;</code>",
        "example": "<code>&lt;img src='image.jpg' alt='Sample image'&gt;</code>"
    },
    {
        "id": 14,
        "title": "HTML Favicon",
        "content": "A favicon is a small image displayed next to the page title in the browser tab.<h4>How To Add a Favicon in HTML :</h4>You can use any image you like as your favicon.You can also create your own favicon on site like <u>https//www.favicon.cc</u><br><b>Tip:</b> A favicon is a small image, so it should be a simple image with high contrast.<br>A favicon image is desplayed to the left of the page title in the browser tab, like this:<br><img src='data:image/jpeg;base64,' alt='W3Schools.com' ><br> To add a favicon to your website, either save your favicon image to the root directory of your webserver, or create a folder in the root directory called images, and save your favicon image in this folder, A common name for a favicon image in 'favicon.ico' <br>Next add a <code>&lt;link&gt;</code> element to your 'index.html' file, after the <code>&lt;title&gt;</code> element.",
        "example": "<code>&lt;link rel='icon' href='favicon.ico'&gt;</code>"
    },
    {
        "id" :15,
        "title" : "HTML Page Title",
        "content" : "Every web page should have a page title to descript the meaning of the page<br>The <code>&lt;title&gt;</code> element adds a title to your page:<h6>Example</h6><code>&lt;DOCTYPE html&gt;<br>&lt;html&gt;<br>&lt;head&gt;<br>&lt;title&gt;Page Title&lt;/title&gt;<br>&lt;/head&gt;<br>&lt;body&gt;<br>&lt;/body&gt;<br>&lt;/html&gt;</code><br>The title should describe the content and the meaning of the page.<br>The page title is very important for search engine optimization(SEO).The text is used by search engine algorithms to decide the order when listing pages in search results.<br> The <code>&lt;title&gt;</code> element:<ul><li>defines a title in the browser toolbar</li><li>provider a title for the page when it is added to favorites</li><li>displays a title for the page in search results</li></ul><small>So ,try to make the title as accurate and meaningful as possible!</small>",
        "example" : "<code>&lt;title&gt;Page Title&lt;/title&gt;</code>"
    },
]

Json_data_Css = [
    {
        "id": 1,
        "title": "CSS HOME",
        "content": "CSS is the language we use to style an HTML document.<br>CSS describes how HTML elements should be displayed<br>This tutorial will teach you CSS from basic to advanced.<h6>Examples in Each Chapter</h6>This CSS tutorial contains hundreds of CSS examples.<br>With our online editor,you can edit the CSS,and click on a button to view the result.<h6>CSS Example</h6>body { <code>background-color: lightblue;</code>}<h4>Use the Menu :</h4>We recommend reading this tutorial, in the sequence listed in the menu.<br> If you have a large screen, the menu will always be visible.<br> If you have a small screen, you can click on the 'Menu'' button to open the menu.<h4>CSS Template :</h4>We have created some responsive templates for you to use.<br> You can free to modify save , share , and use them in all your projects.",
        "example": " style:'<code>color:black</code>;' <br> CSS Tutorial"
    },
    {
        "id": 2,
        "title": "CSS Introduction ",
        "content": "<p> CSS is the language we use to style a web page.</p><h4> What is CSS?</h4><ul> <li>CSS stands for Cascading Style Sheets</li><li>CSS describes how HTML elements are to be displayed on screen, paper, or in other media </li><li>CSS saves a lot of work.It can control the layout of multiple web pages all at once </li><li>External stylesheets are stored in CSS files</li> </ul>CSS allows you to apply styles to web pages and improve their appearance. You can set text color, align elements, create spacing, and manage the layout to enhance user experience.",
        "example": "use tag <code>{ Style }</code> to apply css.Like a color , padding , margin and much more."
    },
    {
        "id": 3,
        "title": "CSS Syntax",
        "content": "<b> CSS syntax is made up of a selector and declaration block. The selector targets HTML elements, and the declaration block contains properties and values.</b> <br> <br> <b>Selector:</b> The selector is the HTML element you want to style. It can be an element type, class, id, or attribute. <br> <br> <b>Declaration Block:</b> The declaration block is enclosed in curly braces { } and contains one or more declarations. Each declaration consists of a property and a value, separated by a colon :. <br> <br> <b>Property:</b> The property is the aspect of the element you want to change, such as color, font-size, or margin. <br> <br> <b>Value:</b> The value specifies the setting for the property, such as red, 16px, or 10px.",
        "example": "p { color: red; font-size: 16px; }"
    },
    {
        "id": 4,
        "title": "CSS How To",
        "content": "<h2>How To Add CSS</h2><br><h4>CSS can be applied in three ways :</h4> Inline CSS (in the HTML tag)<br> Internal CSS (within the head tag)<br> External CSS (linked as a separate file).<h4>External CSS :</h4>With external CSS, you can change the look of an entire website by changing just one file!<br> Each HTML page must include a reference to the external style sheet file inside the <link> element, inside the head section.<h4>Internal CSS :</h4>An internal style sheet may be used to define a style for a single HTML page.<br> An internal style sheet is defined inside the <style> element, inside the head section.<h4>Inline CSS :</h4>An inline style may be used to apply a unique style for a single element.<br><br> To use inline styles,add the style attribute to the relevant element.The style attribute can contain any CSS property.<h4>Multiple Style Sheets :</h4>If some properties have been defined for the same selector in different style sheets, the value from the last read style sheet will be used.<h4>Cascading Order :</h4>What style will be used when there is more than one style specified for an HTML element?<br> All the styles in a page will 'cascade' into a new 'virtual' style sheet by the following rules, where number one has the highest priority:<br> Inline style (inside an HTML element)<br> External and internal style sheets (in the head section)<br> Browser default",
        "example": ""
    },
    {
        "id": 5,
        "title": "CSS Comments",
        "content": "Comments are used to explain the code,and may help when you edit the source code at a later date.<br> Comments are ignored by browsers. Comments in CSS help to add notes or explanations, ignored by the browser. They start with /* and end with */.",
        "example": "/* This is a comment */"
    },
    {
        "id": 6,
        "title": "CSS Color",
        "content": "CSS offers different ways to specify color: names, hex values, RGB, RGBA, HSL, and HSLA. Colors can be applied to backgrounds, borders, and text.<h4>CSS Color Names :</h4>Tomato, Orange, DodgerBlue, MediumSeaGreen, Gray, SlateBlue, Violet, LightGray, and so on.<h4>CSS Background Color :</h4>The background-color property sets the background color of an element.<h4>CSS Text Color</h4>The color property sets the color of the text.<h4>CSS Border Color :</h4>The border-color property sets the color of the border.",
        "example": "body { color: #333; background-color: #f5f5f5; }"
    },
    {
        "id": 7,
        "title": "CSS Background",
        "content": "The CSS background properties are used to add background effects for elements.<br> In these chapter,you will learn about the following CSS background properties:<ul> <li>background-color</li> <li>background-image</li> <li>background-repeat</li> <li>background-attachment</li> <li>background-position</li> <li>background (shorthand property)</li> </ul><h4>CSS background_color :</h4>The <code> backgroud-color </code> property specifies the background color of an element.<h4>CSS background-image :</h4>The <code> background-image </code> property specifies an image to use as the background of an element.<h4>CSS background-repeat :</h4>The <code> background-repeat </code> property specifies if/how a background image will be repeated.<h4>CSS background-attachment :</h4>The <code> background-attachment </code> property specifies whether a background image should scroll or be fixed (will not scroll with the rest of the page).<h4>CSS background-position :</h4>The <code> background-position </code> property specifies the position of a background image.<h4>CSS background (shorthand property) :</h4>The <code> background </code> property is a shorthand property for:<ul> <li>background-color</li> <li>background-image</li> <li>background-repeat</li> <li>background-attachment</li> <li>background-position</li> </ul> <h4>Opacity/Transparency :</h4>The <code>opacity</code> property specifies the opaqueness/transparency of an element. It can take a value from 0.0 to 1.0. The lower value makes the element more transparent. 0.0 is completely transparent, and 1.0 is completely opaque.",
        "example": "<h6>Example for image:</h6> body { background-image: url('image.jpg'); background-size: cover; <br> <h4>Example for color:</h4> <br> body {background-color:''red} <br><h4>Example for opacity:</h4> <br> div {background-color: green;,opacity: 0.3;} }"
    },
    {
        "id": 8,
        "title": "CSS Borders",
        "content": "The CSS border properties allow you to specify the style , width,and color of an element's border.<h4>CSS Border Style :</h4>The <code>border-style</code> property specifies what kind of border to display.<h4>CSS Border Width :</h4>The <code>border-width</code> property specifies the width of the four borders.<h4>CSS Border Color :</h4>The <code>border-color</code> property specifies the color of the four borders.<h4>CSS Border (shorthand property) :</h4>The <code>border</code> property is a shorthand property for:<ul><li>border-width</li> <li>border-style (required)</li> <li>border-color</li></ul><h4>CSS Border-Radius :</h4>The <code>border-radius</code> property is used to add rounded borders to an element.<h4>CSS Border-Collapse</h4>The <code>border-collapse</code> property sets whether the table borders should be collapsed into a single border or not.",
        "example": "p.dotted {border-style : dotted;} <br>p.dashed {border-style:} dashed; <br>p.solid {border-style: solid;} <br>p.double {border-style: double;} <br>p.groove {border-style: groove;} <br>p.ridge {border-style: ridge;} <br>p.inset {border-style: inset;} <br>p.outset {border-style: outset;} <br>p.none {border-style: none;} <br>p.hidden {border-style: hidden;} <br>p.mix {border-style: dotted dashed;} <br>p.border {border: 5px solid red;} <br>div {border: 2px solid tomato; border-radius: 5px;}"
    },
    {
        "id": 9,
        "title": "CSS Margin",
        "content": "Margins are used to create space around elements,outside of any defined borders.<h4>CSS Margins :</h6>The CSS <code>margin</code> properties are used to create space around elements,outside of any defined borders.With CSS,you have full control over the marigns.There are properties for setting the margin for each side of an element (top,right,bottom,and left).<h6>Margin - Individual Sides :</h6>CSS has properties for specifying the margin for each side of an element:<ul><li><code>margin-top</code></li> <li><code>margin-right</code></li> <li><code>margin-bottom</code></li> <li><code>margin-left</code></li></ul><h4>Margin - Shorthand Property</h4>To shorten the code,it is possible to specify all the margin properties in one property.<h4>Margin - Shorthand Property</h4>The <code>margin</code> property is a shorthand property for the following individual margin properties:<ul><li><code>margin-top</code></li> <li><code>margin-right</code></li> <li><code>margin-bottom</code></li> <li><code>margin-left</code></li> </ul><h4>Margin -Auto :</h4>You can set the margin property to auto to horizontally center the element within its container. The element will then take up the specified width, and the remaining space will be split equally between the left and right margins.<h4>The auto Value :</h4>You can set the margin property to <code>auto</code> to horizontally center the element within its container. The element will then take up the specified width, and the remaining space will be split equally between the left and right margins.<h6>Example</h6><code>div { width: 300px; margin: auto; background-color: #4CAF50;}</code><h4>The inherit Value :</h4>This example lets the left margin of the <code>p class='ex1'</code> element be inherited from the parent elements <code>(div):</code><h6>Example</h6>Use of the inherit value:<br><code>div { border: 1px solid red; margin-left: 100px; } <br>p.ex1 { margin-left: inherit; }</code>",
        "example": "div { margin: 20px; }"
    },
    {
        "id": 10,
        "title": "CSS Padding",
        "content": "Padding is used to create space around an element's content,inside of any defined borders.<h4>CSS Padding :</h4>The CSS <code> padding</code> properties are used to generate space around an element's content,inside of any defined borders.<br>With CSS,you have full contril over the padding . There are properties for setting the padding for each side of an element(top,right,bottom,and left)<h4>Padding - Individual Sides</h4>CSS has properties for specifying the padding for each side of an elements:<ul><li><code>padding-top</code></li><li><code>padding-right</code></li><li><code>padding-bottom</code></li><li><code>padding-left</code></li></ul>All the padding properties can have the following values:<ul><li>length-specifies a padding in px,pt ,cm ,etc</li><li>percentage-specifies a padding in %</li><li>inherit-inherits the padding from its parent element</li></ul><h4>Padding - Shorthand Property :</h4>To shorten the code,it is possible to specify all the padding properties in one property.<br> The <code>padding</code> property is a shorthand property for the following individual padding properties:<ul><li><code>padding-top</code></li><li><code>padding-right</code></li><li><code>padding-bottom</code></li><li><code>padding-left</code></li><li><code></code></li></ul>So, here is how it works:<br>If the <code>padding</code> property has four values:",
        "example": "p { padding: 15px; }"
    },
    {
        "id": 11,
        "title": "CSS Height/Width",
        "content": "The CSS <code>height</code> and <code>width</code> properties are used to set the  height and width of an element.<br>The CSS <code>max-width</code> property is used to set the maximun width of an element.<br>The CSS <code>max-height</code> property is used to set the maximun height of an element.<br>The CSS <code>min-width</code> property is used to set the minimun width of an element.<br>The CSS <code>min-height</code> property is used to set the minimun height of an element.<br>The CSS <code>overflow</code> property specifies what happens if content overflows an element's box.<h4>CSS Setting height and width :</h4>The <code>height</code> and <code>width</code>  properties are used to set the height and width of an element.<br>The height and width properties do not include padding border,and margin of the element.<h4>CSS height and width Values</h4>The <code>height</code> and <code>width</code> properties may have the following vlaues:<ul><li><code>auto</code> - This is default.The browser calculates the height and width</li><li><code>length</code> - Specifies the height/width in px,cm etc</li><li><code>percentage</code> - Specifies the height/width in % of the containing block</li><li><code>initial</code> - Sets the height/width to its default value</li><li><code>inherit</code> - The height/width will be inherited from its parent element</li></ul><h4>CSS max-width Property :</h4>The <code>max-width</code> property is used to set the maximum width of an element.<br>The <code>max-width</code> can be specified in length,percentage,or none(no maximun width).<h4>CSS max-height Property :</h4>The <code>max-height</code> property is used to set the maximum height of an element.<br>The <code>max-height</code>can be specified in length,percentage,or none(no maximun height).<h4>CSS min-width Property :</h4>The <code>min-width</code> property is used to set the minimum width of an element.<br>The <code>min-width</code> can be specified in length,percentage,or none(no minimun width).<h4>CSS min-height Property :</h4>The <code>min-height</code> property is used to set the minimum height of an element.<br>The <code>min-height</code>can be specified in length,percentage,or none(no minimun height).<h4>CSS overflow Property</h4>The <code>overflow</code> property specifies what happens if content overflows an element's box.<br>The <code>overflow</code> property can have one of the following values:<ul><li><code>visible</code> - Default.The overflow is not clipped.The content renders outside the element's box</li><li><code>hidden</code> - The overflow is clipped,and the rest of the content will be invisible</li><li><code>scroll</code> - The overflow is clipped,and a scrollbar is added to see the rest of the content</li><li><code>auto</code> - Similar to scroll,but it adds scrollbars only when necessary</li><li><code>inherit</code> - Specifies that the value of the overflow property should be inherited from the parent element</li></ul>",
        "example": "div { width: 200px; height: 100px; }"
    },
    {
        "id": 12,
        "title": "CSS Box Model",
        "content": "All HTML elements can be considered as boxes.<h4>The CSS Box Model :</h6>In CSS,the term 'box model'is used when talking about design and layout.<br> The CSS box model is essentially a box that wraps around every HTML element.It consists of: content , padding.borders and margins <br> <h4>Width and Height of an Element :</h4>In order to set the width and height of an element correctly in all browsers,you need to know how the box model works.<h6>Example</h6>div {<br><code> width : 320px;</code><br><code> height: 200px;</code><br><code> margin: 20px;</code><code>padding: 10px;</code><br><code>border: 1px solid black; </code><br>}",
        "example": "p { margin: 20px; padding: 10px; border: 1px solid black; }"
    },
    {
        "id": 13,
        "title": "CSS Outline",
        "content": "An outline is a line drawn outside the element's border. The outline does not take up space and does not affect the element's total width and height.<h4>CSS Outline :</h4>An outline is a line that is drawn around elements,outside the border.This property specifies the style,width,and color of an outline.The outline property is a shorthand property for the following individual outline properties:<ul><li>outline-style</li><li>outline-color</li><li>outline-width</li><li>outline-offset</li></ul><h6>Example</h6>button<code>{ outline: 2px dashed blue;}</code><br>",
        "example": "button { outline: 2px dashed blue; }"
    },
    {
        "id": 14,
        "title": "CSS Text",
        "content": "The text property includes various styles for fonts, size, color, alignment, decoration, and more, enhancing readability and aesthetics.<h4>Text Color</h4>The <code> color </code> property is used to set the color of the text.The color is specified by:<ul><li>a color name - like 'red'</li><li>a HEX value -like '#FF0000'</li><li>a RGB value - like 'rgb(255,0,0)'</li><li>a HSL value - like 'hsl(0,100%,50%)'</li></ul><h4>Text Color and Background Color</h4>In this example,we define both the background-color property and the <code>color</code> property:<br><h6>Example</h6> ",
        "example": "h1 { text-align: center; color: #333; }"
    },
    {
        "id": 15,
        "title": "CSS Fonts",
        "content": "CSS font properties control font-family, size, weight, style, and line height, allowing for custom typography.<h4>Font Selection is important :</h6>Choosing the right font has a huge impact on how the readers experience a website.<br> The right font can create a strong identity for your brand.<br>Using a font that is easy to read is importent.The font adds value to your text.It is also important to choose color and text size for the font.<h4>Generic Font Families</h4>In CSS there are five generic font families:<ol><li><b>Serif </b>fonts have a small stroke at the edges of each letter.They create a sence of formality and elegance.</li><li><b>Sans-serif</b> fonts have clean lines(no small strokes attached).They create a modern and mimimalistic look. </li><li><b>Monospace</b> fonts -here all the letters have the same fixed width.They create a mechanical look.</li><li><b>Monospace </b>fonts - here all the letters have the same fixed width. They create a mechanical look. </li><li><b>Cursive</b> fonts imitate human handwriting.</li></ol>",
        "example": "p { font-family: Arial, sans-serif; font-size: 16px; }"
    },
    {
        "id": 16,
        "title": "CSS Icons",
        "content": "Icons can be added using CSS with icon libraries like Font Awesome or through custom SVG or images.<h4>How To Add Icons :</h4>The simplest way to add an icon to your HTML page, is with an icon library, such as Font Awesome.<br>Add the name of the specified icon class to any inline HTML element (like <code>&lt;i&gt;</code> or <code>&lt;span&gt;</code>).<h6>Example</h6>To add a home icon, use the <code>&lt;i&gt;</code> element with the class <code>fa fa-home</code>.<br><b>Note :</b><br>You will need to have the Font Awesome library added to your project to use the icon classes. You can add it to your HTML file using a CDN link or by downloading and including the library files in your project.<h4>Font Awesome Icons :</h4>To use the Font Awesome icons, go to <code>Fontawesome.com</code>",
        "example": "<i class='fa fa-home'></i><br><i class='fa fa-search'><br><i class='fa fa-heart'></i><br><i class='fa fa-music'></i>"
    },
    {
        "id": 17,
        "title": "CSS Links",
        "content": "Links can be styled to change color, add effects like underline or bold, and create hover effects when a user hovers over them.<h4>Styling Links :</h4>Links can be style with any CSS property(e.g. <code>color,font-family,background</code>,etc).<h6>Example</h6> a <code>{ color :'pink'; }</code><br> In addition,links can be styled differently depending on what <b>state</b>they are in.<br>The four links states are:<ul><li><code>a:link</code>- a normal,unvisited link</li><li><code>a:visited</code>- a link the user has visited</li><li><code>a:hover</code>- a link when the user hovers over it</li><li><code>a: active</code>- a link the moment it is clicked</li> </ul><h4>Text Decoration :</h4>The <code>text-decoration</code> property is mostly used to remove underlines form links:<h6>Example</h6>a:link <code>{ text-decoration: none; }</code><br>a:visited <code>{ text-decoration: none; }</code><br> ",
        "example": "a { color: blue; text-decoration: none; }"
    },
    {
        "id": 18,
        "title": "CSS Lists",
        "content": "Lists can be styled to change bullet types, spacing, and padding, and add custom markers.<h4>Unordered Lists :</h4><h6>Example</h6><ul><li style{ list-style-type: square; }>list item 1</li><li>list item 2</li><li>list item 3</li></ul>ul{ list-style-type: circle; }<br><h4>Ordered Lists :</h4><h6>Example</h6><ol> <li>list item 1</li><li>list item 2</li><li>list item 3</li></ol><h4>HTML Lists and CSS List Properties</h4>In HTML,there are tow main types of lists:<br><ul><li>unordered lists</li><li>ordered lists</li></ul>The CSS list properties allow you to:<ul><li>Set different list item markers for ordered lists</li><li>Set different list item markers for unordered lists</li><li>Set an image as the list item marker</li><li>Add background colors to lists and list items</li></ul><h4>Different List Item Markers</h4>The <code>list-style-type</code> property specifies the type of list item marker.<br>The following example shows some of the available list item markers:<br><h6>Example</h6>ul.a<code>{ list-style-type: circle; }</code><br>ul.b>code>{ list-style-type: square; }</code>ol.c<code>{ list-style-type: lower-alpha; }</code><h4>An Image as The List Item Marker</h4>The <code>list-style-image</code> property specifies an image as the list item marker:<h6>Example</h6>ul <code>{ list-style-image: url('star.png'); }</code>",
        "example": "ul { list-style-type: square; }"
    },
    {
        "id": 19,
        "title": "CSS Tables",
        "content": "CSS allows you to style table borders, cell padding, alignment, background color, and more.<h4>Table Borders :</h4>To specify table borders in CSS,use the <code>border</code> property.<br> The example below specifies a solid border for { table,th, and td }elements.<h6>Example</h6>table, th, td <code>{ border: 1px solid black; }</code><h4>Collapse Table Borders</h4>The <code>border-collapse</code> property sets whether the table borders should be collapsed into a single border or not.<br><h6>Example</h6>table <code>{ border-collapse: collapse; }</code><h4>Double Borders :</h4>Notice that the table,th,and td elements have double borders:<br><h6>Example</h6><br>table, th, td <code>{ border: 2px double black; }</code><h4>Collapse Double Borders :</h4>The <code>border-collapse</code> property sets whether the table borders should be collapsed into a single border or not.<br><h6>Example</h6><br>table <code>{ border-collapse: collapse; border: 2px double black; }</code>",
        "example": "table { border-collapse: collapse; width: 100%; }"
    },
    {
        "id": 20,
        "title": "CSS Display",
         "content": "The <code>display</code> property defines how an element should be displayed, like block, inline, flex, or grid.<h4>The display Property</h4>The <code>display</code> property is used to specify how an element is shown on a web page. Every HTML element has a default display value depending on what type of element it is. The default display value for most elements is <code>block</code> or <code>inline</code>.<br>The <code>display</code> property is used to change the default display behavior of HTML elements.<h4>Block-level Elements :</h4>A block-level element ALWAYS starts on a new line and takes up the full width available (stretches out to the left and right as far as it can).<h6>Example</h6><ol> <li> <code>div</code></li><li><code>h1 - h6</code></li><li><code>section</code></li><li><code>footer</code></li><li><code>header</code></li><li><code>form</code></li><li><code>p</code></li></ol><h4>Inline Elements :</h4>An inline element DOES NOT start on a new line and only takes up as much width as necessary.<br>Example of inline elements<ul><li><code>span</code></li><li><code>img</code></li><li><code>a</code></li></ul><h4>The display Property Values :</h4>The <code>display</code> property has many values.",
        "example": "div { display: flex; }"
    },
    {
        "id": 21,
        "title": "CSS Max-Width",
        "content": "The max-width property sets the maximum width an element can take, useful for responsive layouts.<br>Using width, max-width and margin:auto;<br>AS mentioned in the previous chapter; a block-level element always takes up the full width available (stretches out to the left and right as far as it can).<br>Setting the width of a block-level element will prevent it from stretching out to the edges of its container. Then, you can set the left and right margins to auto, to horizontally center the element within its container. The element will take up the specified width, and the remaining space will be split equally between the two margins.<br><h6>Example</h6>div <code>{ width: 300px; margin: auto; background-color: #4CAF50; }</code><h4>Using max-width :</h4>The max-width property is used to set the maximum width of an element. The max-width can be specified in length values, like px, cm, etc., or in percent (%) of the containing block, or set to none (this is default. Means that there is no maximum width).<br>The max-width property overrides the width property.<br>Example</h6><br>div <code>{ max-width: 500px; height: 100px; background-color: powderblue; }</code><h4>Using max-width to Control Text Size :</h4>If the max-width of an element is less than the width of the content, the content will automatically wrap and the height of the element will increase.<br>Example</h6><br>div <code>{ max-width: 500px; background-color: powderblue; }</code>",
        "example": "img { max-width: 100%; height: auto; }"
    },
    {
        "id": 22,
        "title": "CSS Position",
        "content": "The position property controls the positioning of elements, with values like static, relative, absolute, and fixed.<h4>Using width,max-width and margin auto :</h4>As mentioned in the previous chapter; a block-level element always takes up the full width available (stretches out to the left and right as far as it can).<br>Setting the width of a block-level element will prevent it from stretching out to the edges of its container. Then, you can set the left and right margins to auto, to horizontally center the element within its container. The element will take up the specified width, and the remaining space will be split equally between the two margins.<br><h6>Example</h6><br>div <code>{ width: 300px; margin: auto; background-color: #4CAF50; }</code><h4>Using position: absolute :</h4>An element with position: absolute; is positioned relative to the nearest positioned ancestor (instead of positioned relative to the viewport, like fixed).<br>However; if an absolute positioned element has no positioned ancestors, it uses the document body, and moves along with page scrolling.<br>Note: A 'positioned' element is one whose position is anything except static.<h6>Example</h6>div <code>{ position: absolute; top: 80px; right: 0; width: 300px; height: 100px; background-color: #4CAF50; }</code><h4>Using position: fixed :</h4>An element with position: fixed; is positioned relative to the viewport, which means it always stays in the same place even if the page is being scrolled. The top, right, bottom, and left properties are used to position the element.<br>A fixed element does not leave a gap in the page where it is positioned, and will not move when the page is scrolled. This can be useful if you want an element to be visible while scrolling.<h6>Example</h6>div <code>{ position: fixed; bottom: 0; right: 0; width: 300px; background-color: #4CAF50; }</code><h4>Using position: sticky:</h4>An element with position: sticky; is positioned based on the user's scroll position.<br>A sticky element toggles between relative and fixed, depending on the scroll position. It is positioned relative until a given offset position is met in the viewport - then it 'sticks' in place (like position:fixed).<br>Note: Internet Explorer does not support sticky positioning.<h6>Example</h6>div <code>{ position: sticky; top: 0; background-color: #4CAF50; }</code>",
        "example": "div { position: absolute; top: 50px; }"
    },
    {
        "id": 23,
        "title": "CSS Z-Index",
        "content": "The z-index property controls the stacking order of elements, useful when layering content.<h4>The z-index Property</h4>When elements are positioned, they can overlap other elements.<br>The z-index property specifies the stack order of an element (which element should be placed in front of, or behind, the others).<br>An element can have a positive or negative stack order:<ul><li>An element with greater stack order is always in front of an element with a lower stack order.</li><li>Note: z-index only works on positioned elements (position: absolute, position: relative, position: fixed, or position: sticky).</li></ul><h6>Example</h6><br>img <code>{ position: absolute; left: 0px; top: 0px; z-index: -1; }</code><br>img <code>{ position: absolute; left: 40px; top: 20px; z-index: 1; }</code>",
        "example": "div { position: absolute; z-index: 10; }"
    },
    {
        "id": 24,
        "title": "CSS Overflow",
        "content": "The CSS <code>overflow</code> property controls what happens to content that is too big to fit into an area.<h4>CSS Overflow :</h4>The <code>overflow</code> property specifies whether to clip the content or to add scrollbars when the content of an element is too big to fit in the specified area.<br>The <code>overflow</code> property has the following values:<ul><li><code>visible</code>- Default,The overflow is not clipped.The content renders outside the element's box.</li><li><code>hidden</code>- The overflow is clipped,and a rest of the content will be invisible</li><li><code>scroll</code>- The overflow is clipped, and a scrollbar is added</li><li><code>auto</code>- Similar to scroll, but a scrollbar is added only when necessary</li></ul><h4>overflow:Visible :</h4>By default,the overflow is <code>visible</code>, meaning that it is not clipped and it renders outside the element's box<h6>Example</h6>div <code>{ overflow: visible; }</code><br>div <code>{ overflow: hidden; }</code><br>div <code>{ overflow: scroll; }</code><br>div <code>{ overflow: auto; }</code><h4>overflow:hidden :</h4>With the <code>hidden</code> value,the overflow is clipped,and the rest of the content is hidden<h6>Example</h6>div<code>{ overflow: hidden; }</code><h4>overflow:scroll :</h4>Setting the value to scroll, the overflow is clipped and a scrollbar is added to scroll inside the box.Note that this will add a scrollbar both horizontally and vertically(even if you do not need it)<h6>Example</h6>div<code>{ overflow: scroll;}</code><h4>overflow:auto :</h4>The <code>auto</code>value is similar to scroll,but it adds scrollbars only when necessary<h6>Example</h6>div<code>{ overflow: auto;}</code><h4>overflow-x and overflow-y :</h4>The <code>overflow-x</code> and <code>overflow-y</code> properties specifies whether to change the overflow of content just horizontally or vertically (or both):<br><code>overflow-x</code> specifies what to do with the left/right edges of the content.<br><code>overflow-y</code> specifies what to do with the /bottom edges of the content.<br><h6>Example</h6><br>div<code>{ overflow-x:hidden}</code><br><code>overflow-y:scroll</code>",
        "example": "div { overflow: scroll; }"
    },
    {
        "id": 25,
        "title": "CSS Float",
        "content": "The float property allows elements to be placed left or right within a container, often used for layouts.<h4>The Float Property :</h4>The <code>float</code> property is used for positioning and formatting content e.g let an image float left to the text in a container:<br> The <code>float</code> property can have one of the following value:<ul><li><code>left</code> -The element float to the left of its container</li><li><code>right</code>- The element floats to the right of its container</li><li><code>none</code>- The element does not float (will be displayed just where it occurs in the text).The is default)</li><li><code>inherit</code>- The element inherits the <code>float</code> property can be used to wrap text around images.</li></ul><h6>Example- float:right; </h6>The following example specifies that an image should float to the right in a text:<h6>Example- float:left;</h6>The following example specifies that an image should float to the left in a text:<h6>Example- float:none;</h6>The following example specifies that an image should not float:<h6>Example- float:inherit;</h6>The following example specifies that an image should inherit the float value of its parent:<h4>The clearfix Hack :</h4>The clearfix hack is a technique used in CSS to contain floats.It is used to force an element to recognize its floated children and to adjust its height accordingly.This is necessary because floated elements are removed from the normal flow of the document,which can cause their parent elements to collapse.<br>The clearfix hack uses a combination of the <code>:after</code> pseudo-element and the <code>clear</code> property to create a new block-level element that clears the floats.This element is then hidden using the <code>content</code> and <code>display</code> properties.<br>Here is an example of how to use the clearfix hack:<h6>Example</h6><code>.clearfix:after { content: ''; display: table; clear: both; }</code><br>In this example,the <code>.clearfix</code> class is applied to the parent element of the floated elements.The <code>:after</code> pseudo-element is used to create a new block-level element that clears the floats.The <code>content</code> property is set to an empty string to hide the element,and the <code>display</code> property is set to <code>table</code> to make it a block-level element.Finally,the <code>clear</code> property is set to <code>both</code> to clear both the left and right floats.<br>By using the clearfix hack,you can ensure that the parent element of the floated elements correctly contains them and adjusts its height accordingly.",
        "example": "img { float: left; }"
    },
    {
        "id": 26,
        "title": "CSS Inline-Block",
        "content": "<h4>CSS Layout - display:inline-block :</h4><b>The display: inline-block Value</b><br>Compared to <code>display : inline</code>, the major difference is that <code>display: inline-block</code> allows to set a width and heigh one the element.<br> Also,with <code>display : inline-block</code>, the top and bottom margins/paddings are respected,but with <code>display: inline</code> they are not.<br> Compared to <code>display: block</code>, the major different is that <code>display:inline-block</code> does not add a line-break after the element, so the element can sit next to other elements.<br> The following example shows the different behavior of <code>display : inline, display : inline-block </code> and <code>display: block</code>:<br><h6>Example</h6><br>span.a<code>{ display :inline;</code><br><code>width:100px</code><br><code>height:100px</code><br><code>padding:5px</code><br><code>border:1px solid blue</code><br><code>background-color:yellow<br>}</code><br>span.b<code>{ display :inline-block;</code><br><code>width:100px</code><br><code>height:100px</code><br><code>padding:5px</code><br><code>border:1px solid blue</code><br><code>background-color:yellow<br>}</code><br>span.c<code>{ display :block;</code><br><code>width:100px</code><br><code>height:100px</code><br><code>padding:5px</code><br><code>border:1px solid blue</code><br><code>background-color:yellow<br>}</code><br><code><span class='a'>This is span A.</span></code><br><code><span class='b'>This is span B.</span></code><br><code><span class='c'>This is span C.</span></code><br>Note: The <code>display: inline-block</code> value is not supported in Internet Explorer 7 and earlier versions.<br> <h6>Using inline-block to Create Navigation Links</h6><br>One common use for <code>display: inline-block</code> is to display list items horizontally instead of vertically.The following example creates horizontal navigation links:",
        "example": "span { display: inline-block; width: 100px; }"
    },
    {
        "id": 27,
        "title": "CSS Align",
        "content": "Align properties like vertical-align and text-align help to position elements vertically and horizontally.<h4>Center Align Elements :</h4> To horizontally center a block element (like <div>), use <code>margin: auto;</code> <br> Setting the width of the element will prevent it from stretching out to the edges of its container.<br>The element will then take up the specified width,and the remaining space will be split equally between the two margins:<br><h6>Example</h6>.center<code>{ margin: auto; width: 300px; background-color: #4CAF50; }</code><h4>Center Align Text :</h4>To just center the text inside an element , use <code>text-align: center;</code><h6>Example</h6>.center<code>{ text-align: center; }</code><br><code>border: 3px solid #f1f1f1;}</code><h4>Center an Image :</h4>To center an image, set left and right margin to <code>auto</code> and make it into a <code>block</code> element:<h6>Example</h6>img { margin-left: auto; margin-right: auto; display: block; }<h4>Left and Right Align - Using positon :</h4>One method for aligning elements is to use <code>position: absolute;</code> and <code>left: 0;</code> or <code>right: 0;</code><h4>Center Vertically - Using padding :</h4>There are many ways to center an element vertically in CSS,A simple solution is to use top and bottom <code>padding</code><h6>Example</h6>.center<code>{ padding-top: 25px; padding-bottom: 25px; }</code> <br> To center both vertically and horizontally, use <code>padding</code>and <code>text-align:center</code><h6>Example</h6>.center<code>{ padding:70px 0; text-align: center; }</code><h4>Center Vertically - Using line-height :</h4><br>Another trick is to use the <code>line-height</code> property with a value that is equal to the <code>height</code> property:<h6>Example</h6>.center<code>{ line-height : 200px;</code><br><code>height: 200px;</code><br> <code>border: 3px solid #f1f1f1;</code><br><code>text-align: center;}</code><h4>Center Vertically- Using positon & transfrom :</h4>If <code>padding</code> and <code>line-height</code> are not options,another solution is to use positioning and the <code>transform</code> property<h6>Example</h6>.center<code>{ position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); }</code>",
        "example": "p { text-align: center; }"
    },
       {
      "id": 28,
      "title": "CSS Combinators",
      "content": "CSS combinators describe the relationship between two selectors (e.g., descendant, child, sibling).<br> A CSS selector can contain more than one simple selector.Between the simple selectors, we can include a combinator. <br> There are four different combinators in CSS:<ul><li>Descendant comninator(space)</li><li>Child combinator(>)</li><li>Adjacent sibling combinator(+)</li><li>General sibling combinator(~)</li></ul><h4>Descendant Combinator :</h4>The descendant combinator matches all elements that are descendants of a specified element. The following example selects all <p> elements inside <code>div</code> elements:<h6>Example</h6> div p { <code>color: blue;</code> } /* all p elements inside div */<h6>Child Combinator(>)</h6><br>The child combinator selects all elements that are the children of a specified element.<br>The following example select all <p> element that are childern of a <div> element:<h6>Example</h6> div > p { <code> background-color: yellow;</code>}<h4>Subsequent-sibling Combinator(~) :</h4>The subsequent-sibling combinator selects all elements that are next siblings of a specified element.The following example selects all <p> elements that are next siblings of <div> elements:<br> div ~ p{ <code> background-color: yellow;</code> }",
      "example": "div p { color: blue; } /* all p elements inside div */"
    },
    {
      "id": 29,
      "title": "CSS Pseudo-Classes",
      "content": "CSS pseudo-classes are keywords added to selectors that specify a special state of the selected elements (e.g., :hover, :active).<h4>What are Pseudo-classes?</h4>A pseudo-class is used to define a special state of an element.<br>For example, it can be used to:<ul><li>Style an element when a user moves the mouse over it</li><li>Style visited and unvisited links differently</li><li>Style an element when it gets focus</li><li>Style valid/invalid/required/optional form elements</li></ul><h4>Syntax :</h4>The syntax of pseudo-classes: <br> selector:pseudo-class { <code>property: value;</code>}<br><h4>Anchor Pseudo-classes :</h4>Links can be displayed in different ways:<h6>Example</h6>a:link <code>{ color: blue; }</code><br>a:visited <code>{ color: purple; }</code><br>a:hover <code>{ color: green; }</code><br><h4>Pseudo-classes and HTML Classes :</h4> Pseudo-classes can be combined with HTML classes:<br>When you hover over the link in the example, it will change color:<h6>Example</h6>a.highlight:hover{ <code>color: red;</code> }<h4>Hover on <div> :</h4>An example of using the <code>:hover</code> pseudo-class on a <div> element:<h6>Example</h6>div:hover { <code>background-color: blue;</code>}<h4>Simple Tooltip Hover :</h4>Hover over a <div> element to show a <p> element (like a tooltip):<br><h6>Example</h6>p {<code>display: none;</code>}<br>div:hover p { <code> display: block;  </code>}<h4>CSS - The :first-child Pseudo-class :</h4>The <code>:first-child</code> pseudo-class matches a specified element that is the first child of another element<h4>Match the first <p> element :</h4>In the following example, the selector matches any <p> element that is the first child of any element:<h6>Example</h6>p: first-child { <code>color: blue;</code> }<h4>Match the first <i> element in all <p> elements</h4>In the following example, the selector matches the first <i> element in all <p> elements:<h6>Example</h6>p i:first-child { <code>color: blue;</code> }<h4>CSS - The :lang Pseudo-class :</h4>The <code>:lang</code>pseudo-class allows you to define special rules for different languages.<br>In the example below, <code>:lang</code> defines the quotation marks for <q> elements with lang='no':",
      "example": "a:hover { color: green; }"
    },
    {
      "id": 30,
      "title": "CSS Pseudo-Elements",
      "content" : "<h4>What are Pseudo-Elements?</h4>A CSS pseudo-element is used to style specific parts of an elements.<br> For Example,it can be used to:<ul><li>Style the first letter or line, of an element</li><li>Insert content before or after an element</li><li>Style the markers of list items</li><li>Style the viewbox behind a dialog box</li></ul><h4>Syntax :</h4>selector:pseudo-element { <code>property :value </code>}<h4> The ::first-line Pseudo-element.The <code>::fisrt-line</code> pseudo-element is used to add a special effect to the first line of a text.</h4><h6>Example</h6><br>p::first-line { <code>color: red;</code> }<h4>The ::first-letter Pseudo-element :</h4>The <code>::first-letter</code> pseudo-element is used to add a special effect to the first letter of a text.<h6>Example</h6>p::first-letter { <code>color: red;</code> }<h4>The ::before Pseudo-element</h4>The <code>::before</code> pseudo-element can be used to insert some content before the content of an element.<h6>Example</h6>p::before { <code>content: 'Read this - ';</code> }<h4>The ::after Pseudo-element :</h4>The <code>::after</code> pseudo-element can be used to insert some content after the content of an element.<h6>Example</h6>p::after { <code>content: ' - Please!';</code> }<h4>The ::marker Pseudo-element :</h4>The <code>::marker</code> pseudo-element selects the markers of list items.<h6>Example</h6>::marker { <code>color: red;</code> }<h4>The ::selection Pseudo-element :</h4>The <code>::selection</code> pseudo-element matches the portion of an element that is selected by a user.<h6>Example</h6>::selection { <code>color: red;</code> }<h4>The ::placeholder Pseudo-element :</h4>The <code>::placeholder</code> pseudo-element selects the placeholder text of an input element.<h6>Example</h6>input::placeholder { <code>color: red;</code> }<h4>The ::backdrop Pseudo-element :</h4>The <code>::backdrop</code> pseudo-element is used to style the backdrop of an element (e.g., behind a dialog box).<h6>Example</h6>dialog::backdrop { <code>background: rgba(0,0,0,0.2);</code> }<h6>The ::cue Pseudo-element</h6>The <code>::cue</code> pseudo-element is used to style the cue of an element (e.g., the track of a video).<h6>Example</h6>track::cue { <code>color: red;</code> }<h4>The ::grammar-error Pseudo-element :</h4>The <code>::grammar-error</code> pseudo-element is used to style the grammar error of an element (e.g., a misspelled word in a text).<h6>Example</h6>::grammar-error { <code>background-color: yellow;</code> }<h4>The ::spelling-error Pseudo-element :</h4>The <code>::spelling-error</code> pseudo-element is used to style the spelling error of an element (e.g., a misspelled word in a text).<h6>Example</h6>::spelling-error { <code>background-color: yellow;</code>}<h4>Multiple Pseudo-Elements :</h4>Several pseudo-element can also be combined.<br> In the following example, the first letter of a paragraph will be red,in an xx-large font size. The rest of the first line will be blue, and in small-caps. The rest of the paragraph will be the default font size and colors:<br><h6>Example</h6> p ::first-letter { <code>color: red </code><br><code>font-size : xx-large</code><br><code>font-size : xx-large</code>}<br> CSS - The ::before Pseudo-element<br> The <code>::before</code> pseudo-element can be used to insert some content before the content of an element. <br> The following example inserts an image before the content of each <code><h1></code> element<br> <h6>Example</h6> h1 ::before { <code>content : url(smiley.gif);</code>}<h4>CSS - The ::after pseudo-element can be used to insert some content after the content of an element. <br> The following example insert an image after the content of each <code> <h1> </code> Element :</h4><h6>Example</h6> h1 ::after { <code>content: url(smiley.gif);</code>}<br> <h4>CSS - The ::marker Pseudo -element :</h6>The <code>::marker</code> pseudo-element selects the markers of list items. <br> The following example style the markers of list items:<h6>Example</h6> ::marker { <code>color : red;</code><br><code>font-size :20px;</code>}<h4>CSS - The ::selection Pseudo-element :</h4><br> The <code> ::selection</code> pseudo-element matches the portion of an element that is selected by a user.<br>The following CSS properties can be applied to <code>::selction : color, background ,cursor,and outline.</code><br> The following example makes the selected text red on a yellow-backgound:<h6>Example</h6>::selection { <code> color :red;</code><br><code>backgorund : yellow;</code>}",
      "example" : "elector::pseudo-element { property: value;}"
    },
    {
      "id": 31,
      "title":"CSS Opacity",
      "content" : "The <code>opacity</code> property specifies the opacity/transparency of an element. The value ranges from 0.0 - 1.0. The lower value makes the element more transparent.<h4>Transparent Image :</h4>The <code>opacity</code> property can take a value from 0.0 - 1.0.The lower the value the more transparent:<br> <div>opacity 0.2</div> <img src=\'https://www.w3schools.com/css/img_forest.jpg' alt=\"Forest\" style=\"width:170px;height:100px;opacity:0.2\" <br><div>opacity 0.5</div> <img src=\'https://www.w3schools.com/css/img_forest.jpg' alt=\"Forest\" style=\"width:170px;height:100px;opacity:0.5;\"<br><div>opacity 1(default)</div> <img src=\'https://www.w3schools.com/css/img_forest.jpg' alt=\"Forest\" style=\"width:170px;height:100px;opacity:1\"<h4>Transparent Hover Effect :</h4>The <code>opacity</code> property can take a value from 0.0 - 1.0. The lower value makes the element more transparent:<br> <div style=\"background-color:#f1f1f1;width:120px;height:200px;opacity:0.4;\">Hover over this div element!</div><h6>Example</h6>img { <code> opacity: 0.5;</code>}<br> img:hover { <code>opacity :1.0 </code>}<h6>Example explained </h6>The first CSS block is similar to the code in Example 1. In addition, we have added what should happen when a user hovers over one of the images. In this case we want the image to NOT be transparent when the user hovers over it. The CSS for this is <code> opacity:1;</code>. <br> When the mouse pointer moves away from the image, the image will be transparent again.An example of reversed hover effect:<br> <h6>Example</h6> img:hover { <code>opacity:0.5;</code>}<h4>Transparent Box :</h4>When using the <code>opacity</code> property to add transparency to the background of an element, all of its child elements inherit the same transparency. This can make the text inside a fully transparent element hard to read:<h6>Example</h6>div { <code>opacity : 0.3</code>}<h4>Transparency using RGBA :</h4>If you do not want to apply opacity to child elements, like in our example above, use <b>RGBA</b> color values. The following example sets the opacity for the background color and not the text:<br>You learned from our CSS Colors Chapter, that you can use RGB as a color value. In addition to RGB, you can use an RGB color value with an alpha channel (RGBA) - which specifies the opacity for a color.An RGBA color value is specified with: rgba(red, green, blue, alpha). The alpha parameter is a number between 0.0 (fully transparent) and 1.0 (fully opaque).<br> <h6>Example</h6> div { <code>background : rgba(76,175,80,0.3)</code>}",
      "example" : "img { <code>opacity: 0.5; </code>}<br> img:hover { <code> opacity :1.0 </code>"
    },
    {
      "id": 32,
      "title" : "CSS Navigation Bar",
      "content" : "<h4>Demo:Navifation Bar</h4><ul><li><a href'default.asp'>Home</a></li>  <li><a href='news.asp'>News</a></li>  <li><a href='contact.asp'>Contact</a></li>  <li><a href='about.asp'>About</a></li> </ul><h4>Navigation Bar :</h4><br>Having easy-to-use navigation is important for any web site.<br> With CSS you can transform boring HTML menus into good-looking navigation bars.<h4>Navigation Bar = List of Links :</h4>A navigation bar needs standard HTML as a base.<br>In our examples we will build the navigation bar from a standard HTML list.<br>A navigation bar is basically a list of links, so using the <ul> and <li> elements makes perfect sense:<br><h6>Example</h6><ul>  <li><a href'default.asp'>Home</a></li>  <li><a href='news.asp'>News</a></li>  <li><a href='contact.asp'>Contact</a></li>  <li><a href='about.asp'>About</a></li> </ul><h6>Example explained:</h6><ul><li><code>list-style-type: none;</code>- Removes the bullets.A navigation bar does not need list markers</li><li>Set <code>margin: 0;</code> and <code> padding :0;</code> to remove browser default setting</li><ul>The code in the example above is the standard code used in both vertical, and horizontal navigation bars, which you will learn more about in the next chapters.",
      "example" : "<ul>  <li><a href'default.asp'>Home</a></li>  <li><a href='news.asp'>News</a></li>  <li><a href='contact.asp'>Contact</a></li>  <li><a href='about.asp'>About</a></li> </ul>"
    },
    {
      "id": 33,
      "title" :"CSS Dropdown",
      "content" : "Create a hoverable dropdown menu when the user mouses over the \"Dropdown\" link.<h6>Example</h6><ul><li><a href='default.asp'>Home</a></li>  <li><a href='news.asp'>News</a></li><li class='dropdown'><a href='javascript:void(0)' class='dropbtn'>Dropdown</a><div class='dropdown-content'><a href='#'>Link 1</a><a href='#'>Link 2</a><a href='#'>Link 3</a></div></li></ul><h6>Example explained:</h6><ul><li>The <code>.dropdown</code> class defines the dropdown behavior. The <code>.dropdown-content</code> class defines the content that should be shown inside the dropdown menu when it is shown.</li><li>The <code>:hover</code> rule is used to show the dropdown content when the user moves the mouse over the dropdown.</li><li>The <code>display: none;</code> rule is used to hide the dropdown content by default.</li><li>The <code>position: absolute;</code> rule is needed to position the dropdown content.</li><li>The <code>min-width</code> rule is often used to define the width of the dropdown content (depending on the content).</li><li>The <code>z-index</code> rule is used to stack the dropdown content above other elements on the page.</li></ul><h6>HTML) Use any element to open the dropdown content, e.g. a <span>, or a <button> element.<br>Use a container element (like <div>) to create the dropdown content and add whatever you want inside of it.<br>Wrap a <div> element around the elements to position the dropdown content correctly with CSS.<br>CSS) The .dropdown class uses position:relative, which is needed when we want the dropdown content to be placed right below the dropdown button (using position:absolute).<br>The .dropdown-content class holds the actual dropdown content. It is hidden by default, and will be displayed on hover (see below). Note the min-width is set to 160px. Feel free to change this. Tip: If you want the width of the dropdown content to be as wide as the dropdown button, set the width to 100% (and overflow:auto to enable scroll on small screens).<br>Instead of using a border, we have used the CSS box-shadow property to make the dropdown menu look like a 'card'.<br><he :hover selector is used to show the dropdown menu when the user moves the mouse over the dropdown button.</h6>",
     "example": "<button class='dropbtn'>Dropdown</button> <div class='dropdown-content'> <a href='#'>Link 1</a> <a href='#'>Link 2</a> <a href='#'>Link 3</a> </div>"


}
  ]


JavaScript = [
        {
          "id" : 1,
          "title" : "JavaScript Tutorial",
          "content" : "Java is the world's most popular programming language. <br>JavaScript is the programming language of the web.<br> JavaScript is easy to learn.<br>This tutorial will teach you Javascript from basic to advanced<h6>Examples in Each Chapter</h6>With our 'Try it Yourself' editor, you can edit the soucre code and view the result.<h4>Use the Menu :</h4>We recommend reading this tutorial, in the sequence listed in the menu.<br>If you have a large screen, the menu will always be present on the left.<br>If you have a small screen, open the menu by clicking the top menu sign.<h6>Learn by Examples</h6>Examples are better than 1000 words.Example are often easier to understand than text explanations.<br>This tutorial supplements all explanations with clarifying 'try it  Youself' examples<br>If you try all the examples, you will learn a lot about JavaScript, in a very short time!<h4>Why Study Javascript?</h4>JavaScript is one of the 3 languages all web developers must learn:<ol><li> HTML to define the content of web pages</li><li>CSS to specify the layout of web pages</li><li>JavaScript to program the behavior of web pages</li></ol>",
          "example" : "document.getElementById('demo').innerHTML = 'Hello JavaScript';"
    },
    {
         "id" : 2,
        "title" : "JS Introduction",
        "content" : "<h3>JavaScript Can Change HTML Content</h3>One of many JavaScript HTML Method is <code>getElementById()</code><br>The example below 'finds' an HTML element (with id = 'demo'), and changes the element content (innerHTML) to 'Hello JavaScript<h6>Example</h6>document.getElementById(<code>'demo'</code>).innterHTML = <code>'Hello JavaScript'</code>;<br><small>JavaScript accept both double and single qoutes:</small><h6>document.getElementById(<code>'demo'</code>).innerHTML=<code>'Hello JavaScript'</code>')</h6><h4>JavaScript Can Change HTMl Attritube Values :</h4>In this example Javascript Change the value of the <code>src</code> (source) attribute of an <code>&lt;img&gt;</code> tag<h4>JavaScript Can Change HTML Style (CSS) </h4>Changing the style of an HTML element, is a variant of changing an HTML attribute:<h6>Example</h6>document.getElementById(<code>'demo'</code>).style.fontSize = <code>'34px</code><h4>JavaScript Can Hide HTML Elements :</h4><small>Hiding HTMl elements can be done by changing the <code>display</code> style:</small><h6>Example</h6>document.getElementById(<code>'demo'</code>).style.display=<code>'none'</code>;<h4>JavaScript Can Remove HTML Elements :</h4>Showing hidden HTML elememts can also be done by changing the <code>display</code> style:<br><h6>Example</h6> document.getElementById(<code>'demo'</code>).style.display=<code>'none'</code>;<br><b>Did You Know?</b><br>JavaScript and <u>Java</u> are completely different languages, both in concept and design.<br>JavaScript was invented by Brendan Eich in 1995, and became an ECMA standard in 1997.<br>ECMA-262 is the offical name of the standard, ECMAScript is the offical name of the language.",
        "example" : "document.getElementById('demo').innerHTML = 'Hello JavaScript';"
    },
    {
        "id" : 3,
        "title" : "JS Where To",
        "content" : "The <code>&lt;script&gt;</code> Tag<br>In HTML, JavaScript code is inserted between <code>&lt;script&gt;</code> and <code>&lt;/script&gt;</code> tags.<h6>Example</h6> <code>&lt;script&gt;</code><br>document.getElementById(<code>'demo'</code>).innerHTML= <code>'My First Hello' </code><br><code>&lt;/scirpt&gt;</code><br><small>Old JavaScript example may use a type attribute:</small><br><code>&lt;script type='text/javascript'&gt;</code><small><h4>JavaScript Function and Events :</h4>A JavaScript <code>function</code> is a block of JavaScript code, that can be executed when 'called' for.<br>For example, a function can be called when an <b>event</b> occurs, like when the user clicks a button<br>You will learn much more about function and events in later chapters.<h4>JavaScript in <code>&lt;head&gt;</code> or <code>&lt;body&gt;</code> :</h4You can place any number of scripts in an HTML document.Scripts can be placed in the <code>&lt;body&gt;</code>, or in the <code>&lt;head&gt;</code> section of an HTML page, or in both<h4>JavaScrip in <code>&lt;head&gt;</code> :</h4>In this example, a javascript <code>function</code> is place in the <code>head</code> section of an HTML page.<br>The function is invoked (called) when a button is clicked<h6>Example</h6><code>&lt;!DOCTYPE html&gt;</code><br><code>&lt;html&gt;</code><br><code>&lt;head&gt;</code><br><code>&lt;script&gt;</code><br> function myFunction() { document.getElementById(<code>'demo'</code>).innerHTML=<code>'Paragraph Changed'</code>;}<br><code>&lt;/script&gt;</code><br><code>&lt;/head&gt;</code><br><code>&lt;body&gt;</code><br><code>&lt;h2&gt;</code> Demo JavaScript in Head <code>&lt;/h2&gt;</code><br><code>&lt;p id='demo'&gt;</code>A Paragraph<code>&lt;/p&gt;</code><br><code>&lt;button type='button' onclick='myFunction()''&gt;</code> Click Me <code>&lt;/button&gt;</code><br><code>&lt;/body&gt;</code><br><code>&lt;/html&gt;</code<br><h4>JavaScript in <code>&lt;body&gt;</code> :</h4>In this example, a javaScript <code>function</code> is placed in the <code>&lt;body&gt;</code> section of an HTML page.<br>The function in invoked (called) when a button is clicked:<br><h6>Example</h6><code>&lt;!DOCTYPE html&gt;</code><br><code>&lt;html&gt;</code><br><code>&lt;body&gt;</code><br><code>&lt;h2&gt;</code>Demo JavaScript in Body <code>&lt;/h2&gt;</code><br><code>&lt;p id='demo'&gt;</code> A Paragraph<code>&lt;/p&gt;</code><br><code>&lt;button type='button' onclick='myFunction()''&gt;</code> Click Me <code>&lt;/button&gt;</code><br><code>&lt;/body&gt;</code><br><code>&lt;/html&gt;</code><br><small>Placing script at the bottom of the <code>&lt;body&gt;</code> element improves the display speed,because the page loads faster.</small><h4>External JavaScript :</h4><br>Script can also be pleced in external files<br>External file:myScript.js<br> <code>function</code> myFunction() { document.getElementById(<code>'demo</code>).innerHTML='Paragraph Changed';<br>}<br>External scripts are practical when the same code is used in many different web pages.<br>JavaScript files have the file extension.<b>js</b>.<br>To use an external script, put the name of the script file in the <code>src</code> (source) attribute of a <code>&lt;script&gt;</code> tag:<br><h6>Exmaple</h6><code>&lt;script src='myScript.js'&gt;&lt;/script&gt;</code><br>You cab place an external script reference in <code>&lt;head&gt;</code> or <code>&lt;body&gt;</code> as you like.<br>The script will behave as if it was located exactly where the <code>&lt;script&gt;</code> tag is locted.<br>External scripts cannot contain <code>&lt;script&gt;</code> tags.<br><h4>External JavaScript Advantages :</h4><br>Placing scripts in external files has some advantages:<ul><li>It separates HTML and JavaScript</li><li>It makes the HTML file smaller</li><li>It makes HTML and JavaScript easier to read and maintain</li><li>Cached JavaScript files can speed up page loads<li><br>To add several script files to one page - use several script tags:<br><h6>Example</h6><code>&lt;script src='myScript1.js'&gt;&lt;/script&gt;</code><br><code>&lt;script src='myScript2.js'&gt;&lt;/script&gt;</code><br><b>External References</b><br>An external script can be referenced in 3 different ways:<ul><li>With a full URL{ a full web address }</li><li>With a file path(like /js/)</li><li>Without ant path</li><br>This example user a <b>full URL</b> to link to myScript.js<br><h6>Example</h6><code>&lt;script src='https://www.wexample.com/js/myScript.js'&gt;&lt;/script&gt;</code> <br>This example user a <b>file path</b> to link to myScript.js",
        "example" : "img { <code>opacity: 0.5; </code>}<br> img:hover { <code> opacity :1.0 </code>"  
    },
    {
        "id" : 4,
        "title" : "Js Output",
        "content" : "JavaScript Display Possibilites<br>Javascript can 'display' data in different ways:<ul><li>Writing into an HTML element, using <code>innerHTML</code>.</li><li>Writing into the HTML output using <code>document.write()</code>.</li><li>Writing into an alert box, using <code>alert()</code>.</li><li>Writing into the browser console, using <code>console.log()</code>.</li><br><h4>Using <code>innerHTML</code> :</h4>To access an HTML element, JavaScript can use the <code>document.getElementById(id)</code> method.<br>The <code>id</code> attribute defines the HTML element.The <code>innerHTML</code> property defines the HTML content:<h6>Example</h6><code>&lt;!DOCTYPE html&gt;</code><br><code>&lt;html&gt;</code><br><code>&lt;body&gt;</code><br><code>&lt;h1&gt;</code> My First Web Page<code>&lt;/h1&gt;</code><br><code>&lt;p&gt;</code>My First Paragraph<code>&lt;/p&gt;</code><br><code>&lt;script&gt;</code><br> document.getElementById(<code>'demo'</code>).innerHTML = 3 + 5;<br><code>&lt;/script&gt;</code><br><code>&lt;/body&gt;</code><br><code>&lt;/html&gt;</code><br><small>Changing the innerHTML property of an HTML element is a common way to display data.</small><br><h4>Using document.write() :</h4>For testing purposes, it is convenient to use <code>document.write()</code>:<br><h6>Example</h6><code>&lt;!DOCTYPE html&gt;</code><br><code>&lt;html&gt;</code><br><code>&lt;body&gt;</code><br><code>&lt;h1&gt;</code> My First Web Page<code>&lt;/h1&gt;</code><br><code>&lt;p&gt;</code>My First paragraph.<code>&lt;/p&gt;</code><br><code>&lt;script&gt;</code><br><code>&lt;/body&gt;</code><br><code>&lt;/html&gt;</code><br><small>The document.write() method should only be used for testing</small><br><h4>Using window.alert() :</h4>You can use an alert box to display data<h6>Example</h6><code>&lt;!DOCTYPE html&gt;</code><br><code>&lt;html&gt;</code><br><code>&lt;body&gt;</code><br><code>&lt;h1&gt;</code> My First Web Page.<code>&lt;/h1&gt;</code><br><code>&lt;p&gt;</code>hello world<code>&lt;/p&gt;</code><br><code>&lt;script&gt;</code><br>window.alert(4 + 6)<br><code>/script&lt;&gt;</code><br><code>&lt;/body&gt;</code><br><code>&lt;/html&gt;</code><br>You can skip the <code>window</code> keyword.<br>In JavaScript, the window object is the global scope object.This means that variables, properties, and methods by default belong to the window object. This also means that specifying the <code>window</code> keyword is optional:<br><h6>Example</h6><code>&lt;!DOCTYPE html&gt;</code><br><code>&lt;html&gt;</code><br><code>&lt;body&gt;</code><br><code>&lt;h1&gt;</code> My First Web Page.<code>&lt;/h1&gt;</code><br><code>&lt;p&gt;</code>My First paragraph.<code>&lt;/p&gt;</code><br><code>&lt;script&gt;</code><br>alert(4 + 6);<br><code>&lt;/script&gt;</code><br><code>&lt;/body&gt;</code><br><code>&lt;/html&gt;</code><br><h4>Using console.log() :</h4>For debugging purpose, you can call the <code>console.log()</code> method in the browser to display data.<br><small>You will learn more about debbuing in a later chapter</small><br><h6>Example</h6><code>&lt;!DOCTYPE html&gt;</code><br><code>&lt;html&gt;</code><br><code>&lt;body&gt;</code><br><code>&lt;script&gt;</code><br>console.log(5 + 5)<br><code>&lt;/script&gt;</code><br><code>&lt;/body&gt;</code><br><code>&lt;.html&gt;</code><h4>JavaScript Print :</h4>JavaScript does not have any point object or print methods.<br>You cannot access output devices from javascript.<br>The only exception is that you can call the window.print() method in the browser to print the content of the current window.<h6>Example</h6><code>&lt;!DOCTYPE html&gt;</code><br><code>&lt;html&gt;</code><br><code>&lt;body&gt;</code><br><code>&lt;button onclick='window.print()'&gt;</code>Print this page<code>&lt;/button&gt;</code><br><code>&lt;/body&gt;</code><br><code>&lt;/html&gt;</code>",
        "example" : "Many example show all topic"
    },
    {
        "id" : 5,
        "title" : "Js Statement",
        "content" : "<h4>JavaScript Statements :</h4><h6>Example</h6> let x,y,z; <br>var x = 5; <br>var y = 6; <br>var z = x + y; <br>document.getElementById(<code>'demo'</code>).innerHTML = z;<h4>JavaScript Programs :</h4>A <b>computer program</b> is a list of 'insturction' to be 'executed ' by a computer.<br>In a programming language, these programming instructions are called <b>statements</b><br>A <b>Javascript program</b> is a list of programming <b>statements</b><br>In HTML, Javascript programs are executed by the web browser<br><h4>JavaScript Statements :</h4><br>JavaScript statements are composed of:<br>Values, operators, Experssion , Keywords, and Comments.<br>This statement tells the browser to write 'hello Dolly'.inside an HTML element with id='demo'<h6>Example</h6>document.getElementById(<code>'demo'</code>).innerHTML='Hello Dolly'.;<br>Most Javascript programs contain many JavaScript statements.<br>The statements are executed, one by one, in the same order as they are written.<br>Javascript programs(and JavaScript statements) are often called JavaScript code.<h4>Semicolons :</h4>Semicolons separate JavaScript statements.<br>Add a semicolon at the end of each executable statement:<h6>Example</h6>let a, b, c;<code>&lt;//Declare 3 variables&gt;</code><br>a = 5; <br>b = 6; <br>c = a + b; <br>document.getElementById(<code>'demo'</code>).innerHTML = c; <br><code>&lt;//Add a semicolon at the end of each statement&gt;</code><h4>JavaScript White Space :</h4>JavaScript ignores multiple space.You can add white space to your script to make it more readable.<br>The following lines are equivalent:<br>let person= 'helo'<br>let person='hello'<br>A good practice is to put spaces around operator(= + - * /):<h4>JavaScript Line Length and Line Breaks :</h4>For best readability, programming often like to avoid code lines longer than 80 characters.<br>If a JavaScript statement does not fit on one line, the best place to break it is after an operator:<h6>Example</h6>document.getElementById(<code>'demo'</code>).innerHTML=<br>'hello world'<h4>JavaScript Code Blocks :</h4>Javascript statement can be grouped together in code blocks, inside curly brackets{...}.<br>The purpose of code blocks is to define statement to be executed together.<br>One place you will find statement grouped together in blocks, is in JavaScript functions:<h6>Example</h6>fuction myFun() { <br> document.getElementById(<code>'demo1'</code>).innerHTML='Hello Dolly!';<br>document.getElementById(<code>'demo2'</code>).innerHTML='How are you?';<br>}<br><small>In this tutorial we use 2 space of indentation for code blocks.<br>You will learn more about function later in this tutorial.</small>",
        "example" : "Many example show all topic"
    },
    {
        "id" : 6,
        "title" : "Js Syntax",
        "content" : "<h4>JavaScrip Syntax :</h4>JavaScript syntax is the set of rules, how JavaScript programs are sonstructed<br><code>&lt;//How to create variables&gt;</code><br>var x;<br>let y;<br><code>&lt;//How to use variables&gt;</code><br>x=5;<br>y=6;<br>let z = x + y;<h4>JavaScrip Values :</h4>The JavaScript syntax defines two types of values:<ul><li>Fixed values</li><li>Variable values</li></ul>Fixed values are called <b>Literals.</b>Variable values are called <b>Variables</b><h4>JavaScript Literals :</h4>The two most important syntax rules for fixed values are:<br>1.<b>Numbers</b> are wirtten with or without decimals:<br>10.42<br>1001<br>2.<b>Strings</b> are text, written within double or single quotes:<br>Ali<br>Shan<h4>JavaScript Variables :</h4>In a programming language, <b>variables</b> are used to <b>store</b> data values.<br>JavaScript uses the keywords <code>var</code>, <code>let</code> and <code>const</code> to <b>declare</b> variables.<br>An <b>equal sign</b> is used to <b>assign values</b> to variable.<br>In this example, x is defined as a variable.Then, x is assigned(given) the value 6;<br>let x;<br>x = 6;<h4>JavaScript Operators :</h4>Javascript user <b>arithmetic operators</b>(<code>+ - * /</code>) to <b>compute</b> values:<br>(5+6)*10<h4>JavaScript Expressions :</h4>An Expression is a combination of values, variables, and operator, which computes to a value.<br>The computation is called an evaluation.<br>For example, 5 * 10 evaluates to 50:<br><h6>Example</h6>5 * 10<br>Expression can also contain variable values.<br>x *10 <br>The value can be of various types, such as numbers and strings, For example, 'Jhon' + '' +'Doe', evaluates to 'Jhon Doe':<br>'jhon' + '' + 'Doe'<h4>JavaScript Keywords :</h4>JavaScript <b>keywords</b> are used to identify actions to be performed.<br> The <code>let</code> keyword tells browser to create variable:<br>let x ,y;<br>x = 5 + 6<br>y = x *10;<br>The <code>var</code> keyword also tells the browser to create variable:<br>var x , y;<br>x = 5 + 7;<br>y = x  * 10;<br><small>In these example, using <code>var</code> or <code>let</code> will produce the same result.<br>You will learn more about <code>var</code> and <code>let</code> later in this tutorial.</small><br><h4>JavaScript Comments</h4>Not all JavaScript statements are 'executed'.<br>Code after double slasher <code>//</code> or between <code>/*</code> and <code>*/</code> is treated as a <b>comment.</b><br>Comments are ingored, and will not be executed.<br>let x =5,<code>&lt;// i will be excuted&gt;</code><br><code>&lt;// x=6; i will not be excuted&gt;</code><br><small>You will learn more about comments later in this tutorial</small><br><h4>JavaScript Identifiers /Name</h4><br>Identifiers are JavaScript names.<br>Identifiers are used to name variable and keywords, and function.<br>The rules for legal names are the same in most programming languages.<br>A JavaScript name must begin with:<ul><li>A letter(A-Z or a-z)</li><li>A dollor sign($)</li><li>Or an underscore(_)</li></ul><br>Subsequent character may be letters, digits, undersocres, or dollar signs.<h6>Note</h6><small>Numbers are not allowed as the first character in an identifier<br>This way JavaScript can easily distinguish between a variable and a number</small><h4>JavaScript is Case Sensitive :</h4>All JavaScript identifiter are <b>case sensitive</b><br>The variables <code>lastName</code> and <code>lastname</code>, are two different variables:<br>let lastname, lastname;<br>lastName = 'Doe';<br>lastname = 'Peterson'<br>JavaScript does not interpret <b>LET</b> or <b>Let</b> as the keyword <b>let</b><h4>JavaScript and Camel Case :</h4>Historically, programmer have used different ways of joining multiple words into one variable name:<br><b>Hyphens:</b><br>first-name, last-name, master-card, inter-city<br>Hyphens are not allowed in JavaScript.They are reserved for Subtraction.<br><b>Underscore</b>:<br>first_name,last_name,master_card,inter_city<br><b>Upper Camel Case(Pascal Case):</b><br>FirstName,LastName,MasterCard,InterCity.<br><b>Lower Camel Case:</b><br>JavaScrip programmers tend to use camel case that starts with a lowercase letter:<br>firstName, lastName,masterCard,interCity<h4>JavaScript Characters Set :</h4>JavaScript user the <b>Unicode</b> character set.<br>Unicode covers (almost) all the characters, punctuation,and symbols in the world.<br>For a closer look , please study our <u>Complete Unicode Character Set</u>",
        "example" : "Many example show all topic"
    },
    {
        "id" : 7,
        "title" : "Js Comments",
        "content" : "JavaScript comments can be used to explain JavaScript code, an to make it more readable.<br>JavaScript comments can also be used to prevent execution, when testing alternative code.<h4>Single Line Comment :</h4>Single line comments start withh <code>//</code>.<br>JavaScript comments can also be used to prevent execution, when testing altrnative code.<h4>Single Line Comment :</h4>Single line comments start with <code>//</code>.<br>Any text btw <code>//</code> and the end of the line will be ignored by JavaScript(will not be executed).<br>This example user a single-line comment before each code line:<h6>Example</h6><code>&lt;// Change heading&gt;</code><br>document.getElementById(<code>'myH1'</code>).innerHTML = 'My First Page';<br><code>&lt;// Change heading&gt;</code><br>This example user a single line comment at the end of each line to explain the code:<h6>Example</h6>let x = 5;<code>&lt;// Declare x , give it the value 5&gt;</code><br>let y = x +3 <code>&lt;// Declare y , give it the value of x + 3&gt;</code><h4>Multi-line-Comments :</h4>Multi-line comments start with <code>/*</code> and each with <code>*/</code>.<br>Any text btw <code>/*</code> and <code>*/</code> will be ignored by JavaScript.<br>This example user a multi-line comments(a comment block) to explain the code:<h6>Example</h6><code>&lt;/* The code below will change the heanding of the page*/&gt;</code><br>document.getElementById(<code>'myH1'</code>).innerHTML = 'My First Page';<br><code>&lt;/* The code above will change the heanding of the page*/&gt;</code><br><small>It is most common to use single line comments.<br>Block comments are often used for formal documentation</small><h4>Using Comments to Prevent Execution :</h4>Using comments to pervent execution of code is suitable for code testing.<br>Adding <code>//</code> in front of a code line changes the code lines from an executable line to a comment.<br>This example user // to pervent execution of one of the code lines:<h6>Example</h6><code>&lt;// document.getElementById('myH1').innerHTML = 'My First Page';&gt;</code>document.getElementById(<code>'myp'</code>).innerHTML='My first paragraph.'<br>This example user a comment block to prevent execution of multiple lines:<h6>Example</h6><code>&lt;/* document.getElementById('myH1').innerHTML = 'My First Page';*/&gt;</code>",
        "example" : "Many example show all topic"
    },
    {
        "id" : 8,
        "title" : "Js Variables",
        "content" : "<h4>JavaScript Variables :</h4><b>Varibles are Containers for Storing Data</b><br>JavaScript Variable can be declared in 4 ways:<ul><li>Automatically</li><li>With the var keyword</li><li>With the let keyword</li><li>With the const keyword</ul>In this first example, <code>x , y</code> and <code>z</code> are undeclared variables.<br>They are automatically declared when first used.<h6>Example</h6>x = <code>5</code>;<br>y = <code>8</code><br>z = x + y;<br><b>Note</b><br>It is considered good programming practice to always declare variables before use.<br>From the example you can guess:<ul><li>x stores the value 5</li><li>y stores the value 8</li></ul><h6>Example using var</h6><br>var x = 5<br>var y = 8<br><b>Note</b>The <code>var</code> keyword was used in all JavaScript code from 1995 to 2015.<br>The <code>let</code> and <code>const</code> keywords were added to JavaScript in 2015.<br>The <code>var</code> keyword should only be used in code written for older browsers.<h6>When to Use var, let, or const?</h6><ol><li>Always declare variables</li><li>Always use <code>const</code> if the value should not be changed(Arrays and Objects)</li><li>Use <code>let</code> if the value should be changed</li></ol><h4>JavaScript Identifiers :</h4>All JavaScript variables must be identified with unique names.<br>These unique names are called identifiers<br>Identifiers can be short names (like x and y) or more descriptive names(age, sum, totalVolume).<br>The general rules for constructing names for variable (unique identifiers) are:<ul><li>Names can contain letters, digits, underscores, and dollar signs.</li><li>Names can contain letters, digits, underscores, and dollar signs.</li><li>Names can also begin with $ and _ (but we will not use it in this tutorial).</li><li>Names are case sensitive (y and Y are different variables).</li><li>Reserved words (like JavaScript keywords) cannot be used as names.</li></ul><br><b>Note</b><br>JavaScript identifers are case-sensitive.<h4>The Assignment Operator :</h4>In Javascript, the equal sign (=) is an 'assignment' operator, not an 'equal to' operator.<br>This different from algebra.The following does not make sense in algebra.<br>x = x +5<br>In JavaScript, however, it makes perfect sence: it assigns the value of x + 5 to x.<br>(It calculates the value of x + 5 and puts the result into x.The value of x is incremented by 5.)<br><h6>Note</h6><br>The 'equal to' operator is written like == in JavaScript.<h4>JavaScript Data Types :</h4>JavaScript variables can hold numbers like 100 and text values like 'John Deo'.<br>In programming, text values are called text strings.<br>JavaScript can handle many types of data, but for nowm just think of numbers and strings.<br>String are written inside double or single qoutes.Numbers are written without qoutes.<br>If you put a number in quotes, it will be treated as a text string.<h6>Example</h6>const pi = 3.14;<br>let person = 'John Doe';<br>let answer = 'Yes i am!'<br><h4>Declaring a JavaScript Variable</h4><br>Creating a variable in JavaScript is called 'declaring' a variable.<br>You declare a JavaScript variable with the <code>var</code> or the <code>let</code> keyword:<br>var carName;<br>or<br> let carName;<br>After the declaration, the variable has no value (technically it is <code>undefiened</code>).<br>To assign a value to the variable, use the equal sign:<br>carName = 'VolVo'<br>You can also assign a value to the variable when you declare it:<br>let carName = 'Volvo'<br>In the example below, we create a variable called <code>carName</code> and assign the value 'Volvo' to it.<br>Then we 'output' the value inside an HTML paragraph with id='demo':<h6>Example</h6><code>&lt;p id='demo'&gt;&lt;/p&gt;</code><br><code>&lt;script&gt;let carName = 'Volvo';document.getElementById('demo').innerHTML = carName;&lt;/script&gt;</code><br><b>Note</b><br>It's a good programming practice to declare all variable at the begining of a script.<br><h4>One Statement,Many Variables</h4><br>You can declare many variable in one statement.<br>Start the statement with <code>let</code> and separate the variable by comma:<h6>Example</h6>let person='John Doe' , carName ='Volvo', price = 200;<br>A declaration can span multiple lines:<h6>Example</h6>let person = 'John Doe',<br>carName = 'Volve',<br>price = 200,<h4>Value= undefined :</h4>In computer programs, variables are often declared without a value.The value can be something that has to be calculated, or something that will be provided later, like user input.<br>A variable declared without a value will have the value <code>undefined</code>.<br>The variable carName will have the value <code>undefined</code> after the execution of this statement:<h6>Example</h6>let carName;<h4>Re-Declaring JavaScript Variables :</h4>If you re-declare a Javascript variable declared with <code>var</code>, it will not lose its value.<br>The variable <code>carName</code> will still have the value 'Volvo' after the execution of these statement:<h6>Example</h6>var carName='Volvo';<br>var varName;<br><b>Note</b><br>You cannot re-declare a variable declared with <code>let</code> or <code>const</code><br>This will not work:<br>let carName= 'Volvo'<br>let carName;<br><h4>JavaScript Arithmetic</h4><br>As with algebra, you can do arithmetic with Javascript variables, using operators like <code>=</code> and <code>+</code><h6>Example</h6>let x = 5 + 3 + 5;<br>You can also add strings, but strings will be concatenated:<h6>Example</h6>let x ='John' + '' + 'Doe'<br><b>Note</b><br>If you put a number in quotes, the rest of the numbers will be treated as strings, and concatenated.<h6>Example</h6>let x = 4 + 3 +5<h4>JavaScript Dollar Sign $ :</h4>Since JavaScript treats a dollar sign as a letter, identifiers containing $ are valid variable names:<h6>Example</h6>let $ = 'Hello world'<br>let $$$ = 2;<br>let $mymoney= 5;<br>Using the dollar sign is not very common in JavaScript, but professional programming often use it as an alias for the main function in a JavaScript library.<br>In the JavaScript libary.<br>In the JavaScript library JQuery <code>$('p');</code> means 'select all p elements'<h4>JavaScript Underscore(_) :</h4>Since JavaScript treats underscore as a letter, identifiers containing _ are valid variable names:<h6>Example</h6>let _lastname = 'Johnson'<br>let _x = 3;<br>Using the underscore is not very common in JavaScript, but a convention among professional programmers is to use it as an alias for 'private(hidden)' variables.",
        "example" : "Many example show all topic"
    },
    {
            "id" : 9,
            "title" : "Js Let",
            "content" : "JavaScript Let<br>The <code>let</code> keyword was introduced in <u>ES6 (2015)</u><br>Variables declared with <code>let</code> have <b>Block Scope</b><br>Variables declared with <code>let</code> must be <b>Declared</b> before use<br>Variable declared with <code>let</code> cannot be <b>Redeclared</b> in the same scope.<h4>Block Scope :</h4>Before ES6 (2015), JavaScript did not have <b>Block Scope</b><br>JavaScript had <b>Global Scope</b> and <b>Function Scope</b><br>ES6 introduction the two new JavaScript keywords:<code>let</code> and <code>const</code>.<br>These two keywords provided <b>Block Scope</b> in JavaScript<h6>Example</h6>Variables declared inside a { } block cannot be accessed from outside the blocks:<br>{ <br>let x =3;<br>}}<code>&lt;// x can NOT be used here&gt;</code><h4>Global Scope :</h4>Variables declared with the <code>var</code> always have <b>Global Scope.</b><br>Variables declared with the <code>var</code> keyword can NOT have block scope:<h6>Example</h6>Variables declared with the <code>var</code> inside a { } block can be accessed from outside the block:<br>{ <br> var x = 2;<br>}<code>&lt;// x can be used here&gt;</code><h4>Cannot be Redeclared :</h4>Variable defined with <code>let</code> can not be redeclared.<br>You can not accidentally redeclare a variable declared with <code>let</code>.<br>With <code>let</code> you can not do this:<br>let x = 'John Doe';<br>let x = 0;<br>Variables defined with <code>var</code> can be redeclared.<br>With <code>var</code> tou can do this:<br>var x = 'John Deo';<br>var x = 0;<br><h4>Redeclaring Variables</h4>Redeclaring a variable using the <code>var</code> keyword can impose problems.<br>Redeclaring a variable inside a block will also redeclare the variable outside the block:<h6>Example</h6>var x = 10;<br><code>&lt;// Here x is 10&gt;</code><br>{ <br> var x = 2;<code>&lt;// here x is 2&gt;</code>}<br>Redeclaring a variable using the <code>let</code> keyword can solve this problem.<br>Redeclaring a variable inside a block will not redeclare the variable outside the block;<br>let x =10;<br><code>&lt;// Here x is 10&gt;</code><br>{ let x =2<br><code>&lt;// here x is 2&gt;</code>}<h4>What is Good :</h4><code>let</code> and <code>const</code> have block scope<br><code>let</code> and <code>const</code> can not be redeclared.<br><code>let</code> and <code>const</code> must be declared before use.<br><code>let</code> and <code>const</code> does not bind to <code>this</code>.<br><code>let</code> and <code>const</code> are not hoisted.<h4>What is Not Good? :</h4><code>var</code> does not have to be declared.<br><code>var</code> is hoisted.<br><code>var</code> binds to this.",
            "example" : "<code>let x = 5;</code>"
    },
    {
            "id" : 10,
            "title" : "Js Const",
            "content" : "<h4>JavaScript Const</h4>The <code>const</code> keyword was intoduced in <u>ES6 (2015)</u>Variable defined with <code>const</code> cannot be <b>Redeclared</b><br>Variables defined with <code>const</code> cannot be <b>Reassigned</b><br>Variable defined with <code>const</code>  have <b>Block Scope</b><h4>Cannot be Reassigned :</h4>A variable defined with the <code>const</code> keyword cannot be reassigned:<h6>Example</h6>const PI=3.14;<br>PI=3.14 <code>&lt;// This will throw an error&gt;</code><br>PI = PI + 10; <code>&lt;// This will also throw an error&gt;</code><h4>Must be Assigned :</h4>JavaScript <code>const</code> variable must be assigned a value when they are declared:<h4>Correct :</h4>const PI= 3.14<h6>Incorrect</h6>const PI;<br>PI = 3.14;<h4>When to use JavaScript Const? :</h4><b>Always declare a variable with <code>const</code> when you know that the value should not changed.</b><br>Use <code>const</code> when you declare:<ul><li>A new Array</li><li>A new Object</li><li>A new Function</li><li>A new RegExp</li></ul><h4>Constant Object and Arrays :</h4>The keyword <code>const</code> is a little misleading.<br>It does not define a constant value.It defines a constant refernce to a value. <br>Because of this you can NOT:<br><ul><li>Reassing a constant value</li><li>Reassing a constant array</li><li>Reassign a constant object</li></ul>But you CAN:<br><ul><li>Change the elements of constant array</li><li>Change the properties of constant object</li></ul><h4>Constant Arrays :</h4><br>You can change the elements of a constant array:<h6>Example</h6><code>&lt;// You can create a constant array&gt;</code><br>const cars = ['Saab' , 'Volvo' ,'BMW'];<br><code>&lt;// You can change an element:&gt;</code><br>cars[0] = 'Toyota';<br><code>&lt;// You can change an element&gt;</code><br>cars.push('Audi')<br>But you can NOT reassign the array:<br>Example<br>const cars = ['Saab' , 'Volvo' , 'BMW']<br>cars = ['Toyata' , 'Volvo' , 'Audi'] <code>&lt;// Error&gt;</code><br><h4><Constant Objects/h4><br>You can change the properties of a constant object:<br><h6>Example</h6><code>&lt;// You can create a const object:&gt;</code><br>const car = { type:'Fiat', model:'500', color:'white'};<br><code>&lt;// You can change a property:&gt;</code><br>car.color = 'red'<br><code>&lt;//You can add a property&gt;</code><br>car.owner = 'Johnson'",
            "example" : "<code>const x = 5;</code>"
    },
    {
            'id' : 11,
            "title" : "Js Operators",
            "content" : "<h4>JavaScript Operator</h4><br>Javascript operators are used to perform different types of mathematical and logical computaions.<br><h6>Examples:</h6><br>The Assignment Operator = assigns values<br>The Addition Operator + adds values<br>The Multiplication Operator * multiplies values<br>The Comparison Operator > compares values<br><h4>JavaScript Assignment Operators</h4><br>The Assignment Operator (=) assigns a value to a variable:<br><h6>Assignment Example</h6><br>let x = 10;<br><h4>JavaScript Addition Operator</h4><br>The Addition Operator (+) adds numbers<br>Adding <br>let x = 5;<br>let y = 6;<br>let z = x + y;<br><h4>JavaScrip Multiplication Operator</h4><br>The Multiplication Operator (*) multiplies numbers:<br><h6>Multiplying</h6><br>let x = 5;<br>let y = 3;<br>let z = x * y;<br><h4>Types of JavaScript Operators</h4><br>There are different types of JavaScript Operators:<ol><li>Assignment Operators</li><li>Arithmetic Operators</li><li>Comparison Operators</li><li>Logical Operators</li><li>Bitwise Operators</li><li>Assignment Operators</li><li>String Operators</li><li>Ternary Operators</li><li>Type Operators</li></ol><br><h4>JavaCript Comparison Operators</h4><br>Arithmetic Operators are used to perform arithmetic on numbers:<br><h6>Arithmetic Example</h6><br>let a = 3;<br>let x = (100 + 50) * a;<br><ul><li>+ Addition</li><li>- Subtraction</li><li>* Multiplication</li><li>/ Division</li><li>** Exponentiation</li><li>++ Increment</li><li>-- Decrement</li><li>% Modulus</li></ul>",
            "example" : "<code>let x = (100 + 50) * a;</code>"
    },
    {
            'id' : 12,
            "title" : "Js Data Types",
            "content" : "<h4>JavaScript Data Types</h4><br>JavaScrip has 8 Datatypes<br>String<br>Number<br>Bigint<br>Boolean<br>Null<br>Undefined<br>Symbol<br>Object<br><h6>The object datatype</h6><br>The object data type can contain both <b>built-in objects</b> and <b>user-defined objects:</b><br>Built-in object types can be:<br> objects,arrays, dates, maps, sets, intarrays, floatarrays, promiser, and more.<h6>Example</h6><code>&lt;// Numbers:&gt;</code><br>let length = 16;<br>let weight = 7.5;<br><code>&lt;// Strings:&gt;</code><br>let color = 'Red'<br><code>&lt;// Booleans&gt;</code> let x = true;<br><code>&lt;// Null&gt;</code> let x = null;<br><code>&lt;// Undefined&gt;</code><br><h4>The Concept of Data Types</h4><br>In programming, data types is an important concept.<br>To be able to operate on variables, it is important to know something about the type.<br>Without data types, a computer cannot safely solve this:<br>let x = 16 + 'Volvo';<br>Does it make any sence to add 'Volvo' to sixteen? Will it produce an error or will it produce a result?<br>JavaScript will treat the example above as:<br>let x = '16' + 'Volvo';<br><b>Note</b><br>When adding a number and a string , javaScript will treat the numbers as a string.<h4>JavaScript Types are Dynamic</h4><br>JavaScript has dynamic types.This means that the same variable can be used to hold different data types:<br><h6>Example</h6><br>let x;<br><code>&lt;//Now x is undefined&gt;</code><br>x =5;<br><code>&lt;//Now x is a number&gt;</code><br>x = 'Volvo';<br><code>&lt;//Now x is a string&gt;</code><br>x = 'John'<br><h4>JavaScript Strings</h4><br>A string (or a text string) is a series of characters like 'John Doe'<br>Strings are written with quotes.You can use single or double quotes<br><h4>JavaScript Numbers</h4><br>All JavaScript numbers are stored as decimal numbers (floating point).<br>Numbers can be written with, or without decimals:<br><h6>Example</h6><br><code>&lt;// With decimals&gt;</code> let x1 = 34.00<br><code>&lt;// Without decimals&gt;</code> let x2 = 34<br><h4>Exponential Notation</h4><br>Extra large or extra small numbers can be written with scientific (exponential) notation:<br><h6>Example</h6><br>let y = 123e5 <code>&lt;// 12300000&gt;</code><br>let z = 123e-5 <code>&lt;// 0.00123000&gt;</code><br><b>Note</b><br>Most programming language have many number types:<br>Whole Number (integers):<br>byte(8-bit), short (16-bit), int(32-bit), long (64-bit)<br>Real numbers (floating-point):<br>float(32-bit) , double (64-bit).<br><b>JavaScript numbers are always one type:<br>double (64-bit floating point).</b><br><h4>JavaScript Biglnt</h4><br>All JavaScript numbers are stored in a 64-bit floating-point format.<br>JavaScript BigInt is a new datatype (ES2020) that can be used to store integer values that are too big to be repersented by a normal JavaScrip Number.<br><h6>Example</h6><br>let x = BigInt('343875285821943478571');<br><h6>JavaScript Booleans</h6><br>Booleans can only have two values <code>ture</code> or <code>false</code><br><h4>JavaScript Arrays</h4><br>JavaScript arrays are written with square brackets.<br>Array items are separated by commas.<br>The following code declares (creates) an array called <code>cars</code>, containing three items (car names):<br><h6>Example</h6><br>const cars = ['Saab', 'Volvo' , 'BMW']<br>Array indexes are zero-based, which means the first item is [0], second is [1], and so on.<br><h4>JavaScript Objects</h4><br>JavaScript objects are written with curly braces <code>{ }</code>.<br>Object properties are written as name:value pairs, separated by commas.<br><h6>Example</h6>const person = { firstName:'John' , lastName:'Doe',age:50,eyeColor:'Blue'}<br>The object(person) in the example above has 4 properties: firstName, lastName, age, and eyeColor.<br><h4>The typeof Operator</h4><br>You can use the JavaScript <code>typeof</code> operator to find the type of a JavaScript variable.<br>The <code>typeof</code> operator return the type of a variable or an expression.<br><h6>Example</h6><br>typeof '' <code>&lt;// returns string&gt;</code><br><h4>Undefined</h4><br>In JavaScript, a variable without a value, has the value <code>underfined</code>.The type is also <code>undefined</code><br><h6>Example</h6><br>let car; <code>&lt;// Value is undefined, type is undefined&gt;</code><br>Any variable can be emptied, by setting the value to <code>undefined</code>.The Type will also be <code>undefined</code><br><h6>Example</h6><br>car = undefined; <code>&lt;// Value is undefined, type is undefined&gt;</code><br><h4>Empty Value</h4><br>An empty value has nothing to do with <code>undefined</code><br>An empty string has both a legal value and a type.<br><h6>Example</h6>let car = ''<code>&lt;// The value is '', the type is string&gt;</code>",
            "example" : "<code>let x = 5;</code>"
    },
    {
            "id" : 13,
            "title" : "Js Functions",
            "content" : "<h4>JavaScrip Functions</h4><br>A JavaScript function is a block of code designed to perform a particular task.<br>A JavaScript function is executed when 'something' invokes it (call it).<br><h6>Example</h6><br><code>&lt;// Function to compute the product of two numbers:&gt;</code><br>function myFunction() {<br>let x = 5;<br>let y = 6;<br>let z = x * y;<br>document.getElementById(<code>'demo'</code>).innerHTML = z;<br>}<br><code>&lt;// Call the function&gt;</code><br><h4>JavaScript Function Syntax</h4><br>A JavaScript function is defined with the <code>function</code> keyword, followed by a <b>name</b>, followed by parentheses().<br>Function names can contain letters, digits, underscores, and dollar signs (same rules as variable).<br>The parentheses may include parameter names separated by commas:<br>(<b>parameter1</b>, <b>parameter2,...</b>)<br>The code to be executed, by the function, is placed inside curly brackets: { }<br>function name(parameter1, paremeter2, paremeter3){ <br> <code>&lt;// code to be executed&gt;</code><br>}<br>Function paremeters are listed inside the paretheses () in the function definition.<br>Function arguments are the values received by the function when it is invoked.<br>Inside the function, the arguments (the paremeter) behave as local variables.<br><h4>Function Invocation</h4><br>The code inside the function will execute when 'something' invokes (calls) the function:<br><ul><li>When an event occurs (when a user clicks a button)</li><li>When it is invoked (called) from JavaScript code</li><li>Automatically (self invoked)</li></ul><br>You will learn a lot more about function invocation later in this tutoria<br><h4>Function Return</h4><br>When JavaScript reaches a <code>return</code> statment, the function will shop executing.<br>If the function was invoked from a statement, JavaScript will 'return' to execute the code after the invoking statement.<br>Function often compute a return value. The return value is 'returned' back to the 'caller'<br><h6>Example</h6><br>Calculate the product of two numbers, and return the result:<br><code>&lt;// Function is called, the return value will end up in x&gt;</code><br>let x = myFun(4, 3);<br>function myFun(a , b) { <br> <code>&lt;// Function returns the product of a and b &gt;</code><br>return a * b;<br>}<br><h4>Why Function</h4><br>With function you can reuse code<br>You can write code that can be used many times.<br>You can use the same code with different arguments, to produce differnet results.<br><h4>The () Operator</h4><br>The () operator invokes (calls) the function:<br><h6>Example</h6>function toCelsius(fahrenheit) { <br> return (5/9)* (fahrenheit-32);<br>}<br>let value = toClesius(77);<br><h4>Note</h4><br>As you see from the example above, <code>toCelsius</code> refers to the function object, and <code>toCelsius()</code> refers to the function result.<br><h4>Function Used as Variable Values</h4><br>Function can be used the same way as you use variable, in all types of formulas, assignments, and calculations.<br><h6>Example</h6><br>Instead of using a variable to store the return value of a function:<br>let x = toCelsius(77);<br>let text = 'The temperature is '+ x + 'Celcius';<br><h6>Local Variables</h6><br>Variables declared within a JavaScript function, become <b>LOCAL</b> to the function. <br>Local variable can only be accessed from within the function.<br><h6>Example</h6><code>&lt;// code here can NOT use carName&gt;</code><br>function myFun() { <br>let carName = 'Volvo';<br><code>&lt;// code here CAN use carName&gt;</code><br>}<br><code>&lt;// code here can NOT use carName&gt;</code><br>Since local variable are only recongoized inside their function, variables eith the same name can be used in different functions.<br>Local variable are created when a function starts, and deleted when the function is completed.",
            "example" : "function myFun() { <br>let carName = 'Volvo';<br><code>&lt;// code here CAN use carName&gt;</code><br>}<br><code>&lt;// code here can NOT use carName&gt;</code>"
    },
    {
            "id" : 14,
            "title" : "Js Object",
            "content" : "<h4>JavaScript Objects</h4><br><b>Real Life Objects</b><br>In real life, <b>Object</b> are things like: houses, cars, people, animals, or any other subjects<br><b>Object Properties</b><br>A real life car has <b>properties</b> like weight and color:<br>car.name = Fiat, car.model = 500, car.weight = 850kg, car.color = white.<br>Car objects have the same <b>properties</b>, but the <b>values</b> differ from car to car.<br><h4>Object Methods</h4><br>A real life car has <b>methods</b> like start and stop:<br>car.start(), car.drive(), car.brake(), car.shop().<br>Car objects have the same <b>methods</b>, but the methods are performed at different times.<br><h4JavaScript Variables></h4><br>JavaScript variable are containers for data values.<br>This code assigns a simple value (fiat) to a variable named car:<br><h6>Example</h6>let car = 'Fiat'<br><h4>JavaScript Objeacts <br>Objects are variables too. But objects can contain many values.<br>This code assign <b>many values</b> (Fiat, 500, white) to an <b>object</b> named car:<br><h6>Example</h6>const car = {type:'Fiat', model:'500' , color:'white'}</h4><br><b>Note:</b><br>It is a common practice to declare objects with the <code>const</code> keyword.<br>Learn more about using <code>const</code> with objects in the chapter.<br><h4>JavaScript Object Definition</h4><br><b>How to Define a JavaScript Object<br><ul><li>Using an Object Literal</li><li>Using the <code>new</code> keyword</li><li>Using an Object Constructor</li></ul></b>",
            "example" : "<code>&lt;// Create an object&gt;</code><br>const person = new Object();<br><code>&lt;// Add Properties&gt;</code><br>person.firstName = 'John';"
    },
    {
            "id" : 15,
            "title" : "JS Events",
            "content" : "<h4>JavaScript Event</h4><br>HTML events are 'things' that happen to HTML elements.<br>When JavaScript is used in HTML pages, JavaScript can 'react' on these events.<br><h4>JavaScript Events</h4><br>An HTML event can be something the browser does, or something a user does.<br>Here are some example of HTML events:<br><ul><li>An HTML web page has finished loading</li><li>An HTML input field was changed</li><li>An HTML button was clicked</li></ul><br>Often, when events happen, you may want to do something.<br>JavaScript lets you execute code when events are detected.<br>HTML allows event handler attributes, <b>with JavaScript code</b>, to be added to HTML elements.<br>With single quotes:<br>With double quotes.<br>In the following example, an <code>onclick</code> attribute (with code), is added to a <code>button</code> element:<br><h6>Example</h6><br> <code>&lt;button onclick='document.getElementById('demo').innerHTML= Data()'&gt;</code>The time is?<code>&lt;/button&gt;</code><br>In the example above, the JavaScript code changes the content of the element with id='demo'.<br>In the next example, the code changes the content of its own element (using <code>this.innerHTML</code>):<br><h6>Example</h6><code>&lt;button onclick='displayDate()'&gt;</code>The time is?<code>&lt;/button&gt;</code><br><h4>JavaScript Event Handlers</h4><br>Event handlers can be used to handle and verify user input, user actions, and browser action:<ul><li>Things that should be done every time a page loads</li><li>Things that should be done when the page is closed</li><li>Action that should be performed when a user clicks a button</li><li>And more...</li></ul><br>Many different methods can be used to let JavaScript work with events:<ul><li>HTML event attibutes can execute JavaScript code directly</li><li>HTML event attributes can call JavaScript functions</li><li>You can assign your own event handler function to html elements</li><li>You can prevent event from being sent or being handled</li><li>And more...</li></ul>",
            "example" : "Show example all Topic"
    },
    {
            "id" : 16,
            "title":"Js Strings",
            "content" : "<h4>JavaScript Strings</h4><br>String are for <b>storing text</b><br>String are written <b>with quotes</b><br><h4>Using Quotes</h4><br>A JavaScrip string is zero or more characters written inside quotes.<br><h6>Example</h6>let text = 'John Doe'<br>You can use single or double qoutes:<br><b>Note</b><br>Strings created with single or double qoutes works the same.<br>There is no different btw the two.<br><h4>Quotes Inside Quotes</h4><br>You can use quotes inside a string, as long as they don't match the quotes surrounding the string.<br><h4>Template Strings</h4><br>Templates were intoduced with ES6 (JavaScript 2016).<br>Templates are string enclosed in backticks (`This is template string`)<br>Template allow single and double qoutes inside a string.<br><b>Note</b><br>Templates are not supported in Internet Explorer.<br><h4>String Length</h4><br>To find the length of a string, use the built-in <code>length</code> property:<br><h6>Example</h6>let text = 'ADFGFGKFGLRVKCLTPTTK'<br>let length = text.length;<br><h4>Escape Characters</h4><br>Because strings must be written within quotes, JavaScrip will misunderstand this string<br>let text = 'We are the so-called'Vikings'from the north.';<br>The string will be chopped to 'We are the so=called'.<br>To solve this problem, you can use an <b>backslash escape character</b>.<br>The backslash escape character (<code>/</code>) turns special characters into string characters:",
            "example" : "let text = 'John Doe'"
    },
    {
            "id" : 17,
            "title" : "Js Numbers",
            "content" : "<h4>JavaScript Numbers</h4><br>JavaScript has only one type of number.Numbers can be written with or without decimals.<br><h6>Example</h6>let x = 3.14; <code>&lt;// A number with decimals&gt;</code><br>let y = 3; <code>&lt;// A number without decimals&gt;</code><br>Extra large or extra small numbers can be written with scientific (exponent) notation:<br><h6>Example</h6><br>let x = 123e5; <code>&lt;// 12300000&gt;</code><br>let y = 123e-5 <code>&lt;// 0.00123&gt;</code><br><h4>javaScript Numbers are Always 64-bit Floating Point</h4><br>Unlike many other programming language, JavaScript does not define different types of numbers , like integers, short, long, floating-point etc.<br>JavaScript numbers are always stored as double precision floating point numbers,, following the international IEEE 754 standard.<br>This format stores numbers in 64 bits, where the number (the fraction) is stored in bits 0 to 51, the exponent in bits 52 to 62 and the sign in bit 63.<br><h4>Integer Percision</h4><br>Integers (numbers without a period or exponent notation) are accurate up to 15 digits<br><h6>Example</h6>let x = 99999999999999999999999<br>The maximun number of decimals is 17<br><h4>Floating Percision</h4><br>Floating point arithmetic is not always 100 percent accurate<br>let x = 0.2 + 0.1<br><h4>Adding Numbers and Strings</h4><br><b>WARNING !!</b><br>JavaScript user the + operator for both addition and concatenation.<br>Number are added.String are concatenated.<br>If you add two numbers, the result will be a numbers:<br><h6>Example</h6><br>let x = 10;<br>let y = 32;<br>let z = x + y;<br>If you add two strings, the result will be a string concatenation<br><h4>Numeric String</h4><br>JavaScript strings can have numeric content:<br>let x = 100;<code>&lt;// x is a number&gt;</code><br>let y = '100' <code>&lt;// y is a string&gt;</code><br>JavaScript will try to convert strings to numbers in all numeric operations.<br><h4>Nan - Not a Number</h4><br><code>NaN</code> is a JavaScript reserved word indicating that a number is not a legal number.<br>Typing to do arithmetic with a non-numeric string will result in <code>NaN</code> (Not a Number):<br><h6>Example</h6><br>let x = 100 / 'Apple'",
            "example" : "let c = 10"
    },
    {
            "id" : 18,
            "title" : "JS Bigint",
            "content" : "<h4>JavaScript BigInt</h4><br>JavaScript <code>BigInt</code> variable are used to store big integer values that are too big to be represented by a normal JavaScript <code>Number</code>.<br><h4>JavaScript Integer Accuracy</h4><br>JavaScript integers are only accurate up to 15 digits:<br><h6>Integer Percision</h6><br>let x = 9999999999999999999999<br>In JavaScript, all numbers are stored in a 64-bit floating-point format (IEEE 754 standard).<br>With this standard, large integer cannot be exactly repersented and will be rounded<br>Because of this, JavaScript can only safely repersent integers.<br><h4>How to Create a BigInt</h4><br>To ceate a <code>BigInt</code>, append n to the end of an integer or call <code>BigInt()</code><br><h6>Example</h6><br>let x = 999999999999999;<br>let y = 999999999999999999n;<br><br>let x = 1234567889123456788n;<br>let y = BigInt(1234792384948882)<br><h4>BigInt: A new JavaScript DataType</h4>The JavaScript <code>typeof</code> a <code>BigInt</code> is 'bigint':<br><h6>Example</h6>let x = BigInt(99999999999999999)<br>let type = typeof x;<br><code>BigInt</code> is the second numeric data type in JavaScript (after <code>Number</code>).<br>With <code>BigInt</code> the total number of supported data types in JavaScript is 8:<br><ol><li>String</li><li>Number</li><li>Bigint</li><li>Boolean</li><li>Null</li><li>Undefined</li><li>Symbol</li><li>Object</li><ol><br><h4>BigInt Operators</h4><br>Operators that can be used on a JavaScript <code>Number</code> can also be used on a <code>BigInt</code><br><h6>BigInt Multiplication Example</h6><br>let x = 90071888834839333344n;<br>let y = 4589749744049433n<br>let z = x * y<br><b>Note</b>Arithmetic between a <code>Bigint</code> and a <code>Number</code> is not allowd (type conversion lose information).<br>Unsigned right shift(>>>) can not be done on a <code>Bigint</code>(it does not have a fixed width).<br><h4>BigInt Decimals</h4><br>A <code>BigInt</code> can not have decimals.<br><h6>BigInt Division Example</h6><br>let x = 5n;<br>let y = x / 2;<br><code>&lt;// Error : Cannot mix BigInt and other types, use explicit conversion.&gt;</code><br><h4>Bigint Hex, Octal and Binary</h4><code>BigInt</code> can also be written in hexadecimal, octal , or binary notation",
            "example" : "All exmaple show top",
    },
    {
            "id" : 19,
            "title" : "Js Arrays",
            "content" : "<h4>JavaScript Arrays</h4><br>An array is a special variable, which can hold more than one value:<br>const cars = ['Saab', 'Volvo'];<br><h4>Why Use Arrays?</h4><br>If you have of items (a list of car names, for example), storing the cars in single variable could look like this:<br>let car1 = 'Saab';<br>let car2 = 'Volvo';<br>However, what if you want to loop through the cars and find a specific one?<br>And what if you had not 3 cars, but 300?<br>The solution is an array!<br>An array can hold values under a single name, and you can access the values by referring to an index number><h4>Creating an Array</h4><br>Using an array literal is the easiest way to create a JavaScript Array.Syntax:<br>const array_name = [item1 , item2, ....];<br><h4>Arrays are Objects</h4><br>Arrays are a special type of objects.The <code>typeof</code> operator in JavaScript returns 'object' for arrays<br>But, Javascript arrays are best described as arrays.<br>Arrays use numbers to access its 'elements'.In this example, <code>person[0]</code> return John:<br><h6>Array:</h6><br>const person = ['John' , 'Doe', 64];<br>Objects use names to access its 'members'.In this example, <code>person.firstName</code> return John:<br><h6>Object:</h6><br>const person = {fisrName : 'John', lastName:'Doe', age:46};<br><h4>Array Element Can Be Objects</h4><br>JavaScript variable can be objects.Arrays are special kinds of objects.<br>Because of this, you can have variables of different types in the same Array.<br>You can have objects in an Array.You can have function in an Array.You can have arrays in an Array:<br>myArray[0] = Data.now<br>myArray[1] = myFunction;<br>myArray[2] = myCars;<br><h4>Array Properties and Methods</h4><br>The real strength of JavaScript arrays are the built-in array properties and methods:<br>cars.length <code>&lt;// Returns the number of elements in the array&gt;</code><br>cars.sort() <code>&lt;// Sorts the array&gt;</code><br>Array methods are covered in the next chapters.<br><h4>The length Property</h4><br>The <code>length</code> property of an array returns the length of an array (the number of array elements).<br><h6>Example</h6><br>const fruits = ['Apple', 'Orange' , 'Mango'];<br>let length = fruits.length;<br>The <code>length</code> property is always one more that the highest array index.",
            "example" : "All exmaple show top",
    },
    {
            "id" : 20,
            "title" : "Js Math",
            "content" : "<h4>JavaScript Math Object</h4><br>The JavaScript Math object allows you to perform mathematical tasks on numbers.<br><h6>Example</h6><br>Math.PI;<br><h6>The Math Object</h6><br>Unlike other objects, the Math object has no constructor.<br>The Math object is static.<br>All methods and properties can be used without creating a Math object first.<br><h4>Math Properties (Constants)</h4><br>The syntax for any Math property is : <codeMath.property></code><br>JavaScript provides * mathematical constants that can be accessed as Math properties:<br><h6>Example</h6><br>Math.E <code>&lt;// returns Euler's number&gt;</code><br>Math.PI <code>&lt;// returns PI&gt;</code><br>Math.SQRT1_2 <code>&lt;// returns the square root of 1/2&gt;</code><br>Math.SQRT2 <code>&lt;// returns the square root of 2&gt;</code><br>Math.LN2 <code>&lt;// returns the natural logarithm of 2&gt;</code><br>Math.LN10 <code>&lt;// returns the natural logarithm of 10&gt;</code><br>Math.LOG2E <code>&lt;// returns the base 2 logarithm of E&gt;</code><br>Math.LOG10E <code>&lt;// returns the base 10 logarithm of E&gt;</code><br><h4>Math Methods</h4><br>The syntax for Math any methods is : <code>Math.method(number)</code><br>Number to Integer<br>There are 4 common methods to round a number to an integer:<br>Math.round(x) Returns x rounded to its nearest integer<br>Math.ceil(x) Return x rounded up to its nearest integer<br>Math.floor(x) Returns x rounded down to its nearest integer<br>Math.trunc(x) Returns the integer part of x (new in ES6)<br><h4>Math,round()</h4><br><code>Math.round(x)</code> returns the nearest integer:<br><h6>Example</h6><br>Math.round(4.6);<br>Math.round(4.4)<br><h4>Math.ceil()</h4><br><code>Math.ceil(x)</code> returns the value of x rounded up to its nearest integer:<br><h6>Example</h6><br>Math.ceil(4.6)<br>Math.ceil(5.3)<br><h4>Math.floor</h4><br><code>Math.floor(x)</code> returns the value of x rounded down to its nearest integer:<br><h6>Example</h6><br>Math.floor(4.6)<br>Math.floor(5.3)<br><h4>Math.trunc()</h4><br><code>Math.trunc(x)</code> returns the integer part of x:<br><h6>Example</h6><br>Math.trunc(4.9);<br>Math.trunc(5.7)<br><h4>Math.sign()</h4><br><code>Math.sign(x)</code> returns if x negative, null or positive:<br><h6>Example</h6><br>Math.sign(-5)<br>Math.sign(0)<br>Math.sign(4)<br><h4>Math.pow()</h4><br><code>Math.pow(x ,y)</code> returns the value of x to the power of y:<br><h6>Example</h6><br>Math.pow(8 , 3)<br><h4>Math.sqrt()</h4><br><code>Math.sqrt()</code> returns the square root of x:<br><h6>Example</h6><br>Math.sqrt(64);<br><h4>Math.abs()</h4><br><code>Math.abs(x)</code> returns the absolute value of x:<br><h6>Example</h6><br>Math.abs(-5);</code><br><h4>Math.sin()</h4><br><code>Math.sin(x)</code> returns the sine (a value btw -1 and 1) of the angle x (given in radians).<br>If you want to use degrees instead of readians ,you have to convert degrees to radians:<br>Angle in radians = Angle in degrees x PI / 180 <code>&lt;// returns 1 (the sine of 90 degrees)&gt;</code><br><h4>Math.cos()</h4><br><code>Math.cos()</code> returns the cosine (a value btw -1 and 1) of the angle x (given in radians).<br>If you want to use degrees instead of redians, you have to convert degrees to radians<br>Angle in radians = Angle in degrees x PI / 180.<br><h6>Example</h6><br>Math.cos(0 * Math.PI / 180); <code>&lt;// returns 1 (the cos of 0 degrees);</code><br><h4>Math.min() and Math.max()</h4><br> <code>Math.min()</code> and <code>Math.max()</code> can be used to find the lowest or highest value in a list of arguments:<br><h6>Example</h6><br>Math.min(0, 150, 43, -433,43,-32);<br><h4>Math.random()</h4><br><code>Math.random()</code> returns a random number between 0 (inclusive), and 1 (exclusive):<br><h6>Example</h6><br>Math.random(); <code>&lt;// returns a random number between 0 and 1&gt;</code>",
            "example" : "Many example show all topic"
    },
    {
            "id" : 21,
            "title" : "Js If Else",
            "content" : "<h4></h4>",
            "example" : "Many example show all topic"}
        
]



@auth_bp.route('/json_html', methods=['GET'])
def json_html():
    return jsonify(tutorials_html)

@auth_bp.route('/css_tutorial', methods=['GET'])
def css_tutorial():
    return jsonify(Json_data_Css)

@auth_bp.route('/js_tutorial', methods=['GET'])
def js_tutorial():
    return jsonify(JavaScript)   

@auth_bp.route('/',methods=['GET','POST'])
def index():
    return "hello"


 
